# 一期逐步操作手册（Windows → 可迁服务器）

> 配套：`设计方案.md`、`工程选型与一期实施方案.md`  
> 机器假设：联想拯救者 R9000P，Windows，RTX 4060（一期 **不用** 本地跑 FLUX）  
> 原则：**一步做完并勾选验收，再做下一步。不要跳步。M1 不通禁止充值生图。**

---

## 0. 怎么用这份文档

1. 全程用 **PowerShell**（开始菜单搜「Windows PowerShell」或「终端」选 PowerShell）。  
2. 每一步有 **操作** 和 **验收**。验收没过，停下来按「未通过」排查，不要继续。  
3. 命令里的 `F:\item` 若你改了仓库路径，整份文档一起替换。  
4. Windows 自带的 `curl` 是别名，**一律写 `curl.exe`**。  
5. 每日开工先执行「日常激活」三行（见 §2.7）。

**进度总表（做完再打勾）**

| 段 | 内容 | 预计 | 是否花钱 |
| --- | --- | --- | --- |
| A | Windows 基础软件 | 0.5–1 天 | 否 |
| B | 仓库 + venv + Docker 打包骨架 | 0.5 天 | 否 |
| C | M0 接口与 Job 骨架 | 1–2 天 | 否 |
| D | M1 最小闭环（Look + RAG + Chat） | 3–4 天 | 0–5 元（DeepSeek） |
| E | M2 2D 生图 | 3–4 天 | 主烧 150–250 元 |
| F | M3 多轮 + Gradio + 熔断 | 2 天 | 余量 |
| G | M4 3D/视频占位 | 0.5 天 | 否 |
| H | 黄金集评测报告 | 1 天 | 计入生图账 |
| I | 迁到 Linux 服务器演练 | 0.5 天 | 否（本机 Docker 先演练） |

---

## 1. 路径与迁徙硬约定（先读再动手）

以后要迁服务器，**从第一天就遵守**，否则 Windows 能跑、Linux 全炸。

| 规则 | 正确 | 错误 |
| --- | --- | --- |
| 数据目录 | 只用环境变量 `STYLEFORGE_DATA_DIR`（默认 `./data`） | 写死 `F:\item\data`、`C:\Users\...` |
| 路径拼接 | `pathlib.Path` | `"data\\" + id` |
| 密钥 | `.env`，不进 git | 密钥写进 py |
| 运行 | 同一份 `Dockerfile` + `compose.yaml` | 把 `.venv` 拷到服务器 |
| 系统依赖 | 写进 Dockerfile 的 `apt-get` | 只在本机「装过就行」 |

**迁徙真正带走的东西只有四样：**

1. 源码 git 仓库  
2. `requirements.lock.txt`（冻结版本）  
3. `Dockerfile` + `compose.yaml`  
4. 服务器上新建的 `.env`（密钥不随仓库走）

`.venv`、`data/`、Windows 注册表、CUDA **都不迁**。

---

# A. Windows 环境安装

## A1. 看磁盘空闲

**操作**

```powershell
Get-PSDrive -PSProvider FileSystem | Select-Object Name, @{N='FreeGB';E={[math]::Round($_.Free/1GB,1)}}
```

**验收**

- 计划放仓库和 venv 的盘（建议 `F:` 或空间大的盘）**空闲 ≥ 20 GB**。  
- 若 `C:` 很满、项目在 `F:`：后面把 pip / HuggingFace 缓存也指到 `F:`（A6）。

**未通过**：先清盘或把仓库放到空闲盘，不要继续装 Python 全家桶。

---

## A2. 安装 Git

**操作**

```powershell
winget install --id Git.Git -e --source winget
```

关掉并重新打开 PowerShell，然后：

```powershell
git --version
```

**验收**：打印 `git version 2.x.x`。

**未通过**：从 https://git-scm.com/download/win 安装，安装勾选 “Git from the command line”。

---

## A3. 安装 Python 3.11（不要用商店版 3.13 当默认）

一期锁定 **3.11**，和后面 Docker `python:3.11-slim` 一致，减少「本机能跑镜像不能跑」。

**操作**

```powershell
winget install --id Python.Python.3.11 -e --source winget
```

**必须勾选/确认**（若用图形安装包）：`Add python.exe to PATH`。

**新开** PowerShell：

```powershell
py -3.11 --version
python --version
```

**验收**

- `py -3.11 --version` 为 `Python 3.11.x`。  
- 若 `python --version` 不是 3.11：以后 **一律用 `py -3.11`** 建 venv，不要用错版本。

**未通过**：设置 → 应用 → 应用执行别名，关掉「应用安装程序」对 `python.exe` 的别名。

---

## A4. 允许本用户运行虚拟环境脚本

**操作**

```powershell
Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
```

选 `Y`。

**验收**

```powershell
Get-ExecutionPolicy -Scope CurrentUser
```

应为 `RemoteSigned`。

---

## A5. 安装 Docker Desktop（为「打包迁服务器」服务）

本机日常开发用 venv 即可。**Docker 用来保证和 Linux 服务器同一运行时。**  
R9000P 请用 WSL2 后端（安装向导默认即可）。

**操作**

1. `winget install --id Docker.DockerDesktop -e --source winget`  
2. 安装结束后 **重启一次电脑**。  
3. 打开 Docker Desktop，等到左下角为绿色 Running。  
4. 设置 → General：勾选 `Use the WSL 2 based engine`。  
5. 设置 → Resources：Memory 建议 ≥ 4 GB（有 16GB 内存可给 6 GB）。

**新开 PowerShell**

```powershell
docker version
docker compose version
```

**验收**

- `docker version` 的 **Server** 段有版本号（不只 Client）。  
- `docker compose version` 有 `v2.x`。

**未通过**

- Server 起不来：打开 Docker Desktop 等 1–2 分钟；BIOS 打开虚拟化（VT-x/AMD-V）。  
- 提示 Hyper-V/WSL：按 Docker 向导启用 WSL2，执行 `wsl --install` 后重启。  
- **一期可以先做完 A6–C，再回头把 Docker 验收补上**，但 **B4 的文件必须写好**，E 段开始前 Docker 必须绿。

---

## A6. 把缓存赶到空闲盘（C 盘小的必做）

项目在 `F:\item` 时建议执行（按你的盘符改）：

```powershell
[Environment]::SetEnvironmentVariable("PIP_CACHE_DIR", "F:\cache\pip", "User")
[Environment]::SetEnvironmentVariable("HF_HOME", "F:\cache\huggingface", "User")
New-Item -ItemType Directory -Force -Path F:\cache\pip, F:\cache\huggingface | Out-Null
```

**新开 PowerShell** 验收：

```powershell
echo $env:PIP_CACHE_DIR
echo $env:HF_HOME
```

两行都有路径即过。

国内下载 HuggingFace 模型慢时，可再设（可选，新开窗口生效）：

```powershell
[Environment]::SetEnvironmentVariable("HF_ENDPOINT", "https://hf-mirror.com", "User")
```

---

## A7. 安装 ffmpeg（一期 Look 不强制，为二期视频和统一环境）

**操作**

```powershell
winget install --id Gyan.FFmpeg -e --source winget
```

新开 PowerShell：

```powershell
ffmpeg -version
```

**验收**：第一行含 `ffmpeg version`。  
**未通过**：可跳过，在步骤 D 不影响摄影 Look；迁服务器时由 Dockerfile 安装，不依赖本机 ffmpeg。

---

## A8. 账号（M1 对话才需要，可先注册不必先充值）

| 账号 | 何时必须有 Key | 地址（以官网为准） |
| --- | --- | --- |
| DeepSeek | D4 起真实 Chat | https://platform.deepseek.com/ |
| 硅基流动 **或** 阿里云百炼 | **E1 才需要** | https://siliconflow.cn/ 或 百炼控制台 |
| LangSmith | 可选，F 段 | https://smith.langchain.com/ |

**验收（先做完注册即可）**：浏览器能打开控制台。  
**禁止**：在 E 段之前把生图 Key 写进代码；M1 可用 Look（0 元）+ DeepSeek 小额。

---

## A 段总验收

```powershell
git --version
py -3.11 --version
docker version
```

三项都有输出（Docker 若延期，至少前两项过）。硬盘空闲仍 ≥ 15 GB。

---

# B. 仓库骨架 + 可迁移打包（先于写业务代码）

## B1. 进入仓库并初始化 Git

**操作**

```powershell
cd F:\item
git status
```

若还不是 git 仓库：

```powershell
cd F:\item
git init
```

**验收**：`git status` 能运行，不报错。

---

## B2. 一次性创建目录和忽略规则

**操作**：在 `F:\item` 创建下面文件（用 Cursor 新建即可）。

**文件 `.gitignore`**

```gitignore
.venv/
__pycache__/
*.py[cod]
.env
data/
*.db
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.langsmith/
*.egg-info/
dist/
build/
.DS_Store
Thumbs.db
```

**说明**：`data/` 不进 git（图和向量库很大）。迁服务器时 **重新 ingest 风格包**，或单独打包 `data/chroma`（可选，非必须）。

**验收**：文件存在于 `F:\item\.gitignore`。

---

## B3. 创建虚拟环境并激活（日常开发用这个，不进 Docker）

**操作**

```powershell
cd F:\item
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python --version
where python
```

**日常激活（每天做）**

```powershell
cd F:\item
.\.venv\Scripts\Activate.ps1
```

提示符前面应出现 `(.venv)`。

**验收**

- `python --version` 为 3.11.x。  
- `where python` 第一行是 `F:\item\.venv\Scripts\python.exe`。

**未通过**：未激活、或系统 Python 抢了 PATH。必须看到 `.venv\Scripts`。

---

## B4. 写入依赖文件（两份：浮动安装 + 锁定迁徙）

**文件 `requirements.txt`**（给人和 Docker 安装用）

```text
# StyleForge 一期 — 与 Docker python:3.11 对齐
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
python-multipart>=0.0.9
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
pyyaml>=6.0,<7.0

langgraph>=0.2,<1.1
langchain>=0.3,<1.1
langchain-openai>=0.2,<1.1
langchain-core>=0.3,<1.1

chromadb>=0.5,<1.1
sentence-transformers>=3.0,<6.0

opencv-python-headless>=4.10,<5.0
pillow>=10.4,<12.0
numpy>=1.26,<3.0

pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
gradio>=5.0,<6.0
scikit-image>=0.24,<1.0
```

先 **不要** 把 `mediapipe` 写进默认文件（部分环境轮子不稳）。评测人脸时再装，见 H 段。

**操作（必须在已激活的 `.venv` 里）**

```powershell
cd F:\item
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
python -m pip install -r requirements.txt
python -m pip freeze > requirements.lock.txt
```

第一次会下载 PyTorch CPU（sentence-transformers 依赖），大约 **1–3 GB**，要一段时间。

**验收**

```powershell
python -c "import fastapi, langgraph, chromadb, cv2, PIL, sentence_transformers; print('ok')"
```

打印 `ok`。且存在 `requirements.lock.txt`，体积应明显大于 `requirements.txt`。

**未通过**

- 超时：换网络，或确认 `PIP_CACHE_DIR`、镜像：  
  `python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple`  
  装完仍要 `pip freeze > requirements.lock.txt`。  
- `cv2` 失败：确认装的是 `opencv-python-headless` 不是缺库的源码编译。

**迁徙要点**：服务器 **优先安装 `requirements.lock.txt`**（版本钉死）。只有 lock 装不上时才退回 `requirements.txt` 并重新 freeze。

---

## B5. 环境变量模板（密钥不进 git）

**文件 `.env.example`**

```env
STYLEFORGE_DATA_DIR=./data
STYLEFORGE_HOST=0.0.0.0
STYLEFORGE_PORT=8000
PROJECT_BUDGET_CNY=500
DAILY_BUDGET_CNY=20

# 编排 LLM（D4 起）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
DEEPSEEK_MODEL=deepseek-chat

# 生图（E 段再填，二选一即可先填一个）
SILICONFLOW_API_KEY=
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1
DASHSCOPE_API_KEY=

# 可选
LANGCHAIN_TRACING_V2=false
LANGCHAIN_API_KEY=
```

**操作**

```powershell
cd F:\item
Copy-Item .env.example .env
```

用编辑器打开 `.env`，D4 前可以空着 Key。

**验收**：存在 `.env`；`git status` 里 `.env` 被 ignore。  
**未通过**：检查 `.gitignore` 是否包含 `.env`。

---

## B6. Dockerfile（Linux 服务器将用这一份）

**文件 `Dockerfile`**

```dockerfile
# 与本机 py -3.11 对齐；linux/amd64 方便云主机
FROM python:3.11-slim-bookworm

ENV PYTHONDONTWRITEBYTECODE=1 \
    PYTHONUNBUFFERED=1 \
    PIP_NO_CACHE_DIR=1 \
    STYLEFORGE_DATA_DIR=/app/data \
    HF_HOME=/app/.cache/huggingface

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends \
        libglib2.0-0 \
        libgomp1 \
        ffmpeg \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.lock.txt /app/requirements.lock.txt
RUN pip install --upgrade pip && pip install -r /app/requirements.lock.txt

COPY app /app/app
COPY app_ui.py /app/app_ui.py
COPY scripts /app/scripts
COPY tests /app/tests

EXPOSE 8000
CMD ["uvicorn", "app.api.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

注意：B6 时 `app/` 可能还不存在。**文件先创建**；镜像要等 C 段有 `app/api/main.py` 再 `docker compose build`。

**文件 `.dockerignore`**

```dockerignore
.venv
.git
data
.env
__pycache__
*.md
.pytest_cache
F:\cache
```

---

## B7. compose.yaml（本机 Docker 与服务器同一文件）

**文件 `compose.yaml`**

```yaml
services:
  api:
    build: .
    image: styleforge-api:phase1
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      STYLEFORGE_DATA_DIR: /app/data
    volumes:
      - ./data:/app/data
    restart: unless-stopped
```

**验收（B 段）**：`Dockerfile`、`compose.yaml`、`.dockerignore`、`requirements.lock.txt`、`.env.example` 五个文件都在 `F:\item`。  
此时 **不必** `docker build`（还没有 `app/api/main.py`）。

---

## B8. 写入「路径只走环境变量」的配置模块（第一个 py）

**文件 `app/__init__.py`**（空文件即可）

**文件 `app/settings.py`**

```python
from pathlib import Path
from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_file=".env", extra="ignore")

    styleforge_data_dir: Path = Path("./data")
    styleforge_host: str = "0.0.0.0"
    styleforge_port: int = 8000
    project_budget_cny: float = 500
    daily_budget_cny: float = 20
    deepseek_api_key: str = ""
    deepseek_base_url: str = "https://api.deepseek.com/v1"
    deepseek_model: str = "deepseek-chat"
    siliconflow_api_key: str = ""
    siliconflow_base_url: str = "https://api.siliconflow.cn/v1"
    dashscope_api_key: str = ""

    @property
    def data_dir(self) -> Path:
        p = self.styleforge_data_dir
        p.mkdir(parents=True, exist_ok=True)
        return p

    @property
    def assets_dir(self) -> Path:
        d = self.data_dir / "assets"
        d.mkdir(parents=True, exist_ok=True)
        return d


settings = Settings()
```

**验收**

```powershell
cd F:\item
.\.venv\Scripts\Activate.ps1
python -c "from app.settings import settings; print(settings.data_dir.resolve())"
```

应打印 `F:\item\data`（或你设置的 DATA_DIR）。

---

## B 段总验收

- 激活 venv 后核心包 `import` 成功。  
- 有 lock 文件、Docker 三件套、`.env` 被忽略。  
- **没有任何** `F:\` 写在业务逻辑里（仅允许你自己在 PowerShell 里 `cd`）。

---

# C. M0 接口与 Job 骨架（不调付费 API）

**本段目标**：curl 能上传（可先 mock）、能建 Job、能查状态；3D/视频返回 501。

按 **C1 → C7** 顺序写代码。每写完一个模块跑该步验收。

建议目录（与设计方案一致）：

```
app/api/main.py
app/schemas/job.py
app/renderers/base.py
app/renderers/photo_look/  （C 段可先 mock）
app/renderers/image_2d/
app/renderers/asset_3d/stub.py
app/renderers/video_clip/stub.py
app/renderers/video_long/stub.py
app/jobs/store.py
app/jobs/worker.py
app/cost/table.py
tests/test_m0_jobs.py
```

## C1. 冻结 Pydantic 信封 `app/schemas/job.py`

**要包含的字段（与设计方案 §6 对齐，名称不要擅自改）**

- `modality`：`image.2d` | `image.photo_look` | `asset.3d` | `video.clip` | `video.long`  
- `style_id`、`prompt`、`input_assets`、`constraints`、`overrides`、`budget_cny_max`  
- Job 响应：`job_id`、`status`（`queued|running|succeeded|failed|cancelled`）、`estimated_cost_cny`、`actual_cost_cny`、`outputs`、`trace`、`error`  
- `error`：`{code, message, retryable}`

**验收**

```powershell
python -c "from app.schemas.job import RenderJob; print('schema ok')"
```

（类名可按你拆分的模型文件调整，但必须能 import。）

---

## C2. `Renderer` 协议 + MockRenderer

`submit` / `poll` / `estimate_cost`；Mock 把一张纯色 PNG 写到 `settings.assets_dir`。

**验收**：单独写一个 `tests/test_mock_renderer.py`：`submit` 后 `poll` 得到 `succeeded` 和存在的文件。

```powershell
pytest tests/test_mock_renderer.py -q
```

通过即过。

---

## C3. SQLite Job 存储（路径：`settings.data_dir / "jobs.db"`）

状态机只允许：`queued → running → succeeded|failed|cancelled`；`failed → running`（retry）。

**验收**：`pytest` 覆盖非法跳转（例如 `succeeded → queued`）必须失败。

---

## C4. 单价表 `app/cost/table.py`

Look mock = 0；未知模型给保守高价。`estimate` 超过 `budget_cny_max` 则 **不调用** renderer。

**验收**：单测：`budget_cny_max=0` 且单价 > 0 → 错误码 `BUDGET_EXCEEDED`。Look 单价 0 可以通过。

---

## C5. FastAPI 路由（全部先挂上，3D/视频 501）

必须实现（空实现也要有路由）：

| 方法 | 路径 |
| --- | --- |
| GET | `/v1/health` |
| GET | `/v1/capabilities` |
| GET | `/v1/styles` |
| GET | `/v1/styles/{style_id}` |
| POST | `/v1/styles/ingest` |
| POST | `/v1/chat` |
| GET | `/v1/chat/{thread_id}` |
| POST | `/v1/assets` |
| GET | `/v1/assets/{asset_id}` |
| GET | `/v1/assets/{asset_id}/file` |
| POST | `/v1/jobs` |
| GET | `/v1/jobs/{job_id}` |
| POST | `/v1/jobs/{job_id}/cancel` |
| POST | `/v1/jobs/{job_id}/retry` |
| POST | `/v1/rag/documents` |
| POST | `/v1/rag/query` |

M0 时：`/v1/styles` 可返回 `[]`；`/v1/chat` 可返回 501 或占位 JSON；**Jobs / health / capabilities 必须真能跑**。

C5 结束时 `capabilities` 应为：

- `image.photo_look`: `ready`（哪怕 backend 是 mock）  
- `image.2d`: `not_ready` 或 `mock`  
- `asset.3d` / `video.clip` / `video.long`: `reserved`

**启动**

```powershell
cd F:\item
.\.venv\Scripts\Activate.ps1
uvicorn app.api.main:app --host 127.0.0.1 --port 8000
```

另开一个 PowerShell：

```powershell
curl.exe -s http://127.0.0.1:8000/v1/health
curl.exe -s http://127.0.0.1:8000/openapi.json | findstr "health"
```

**验收**

- health 返回 JSON，如 `{"status":"ok"}`。  
- `openapi.json` 含 `/v1/jobs`。

---

## C6. 用 curl 走通 Job（mock）

保持 uvicorn 运行。

```powershell
curl.exe -s -X POST http://127.0.0.1:8000/v1/jobs -H "Content-Type: application/json" -d "{\"modality\":\"image.photo_look\",\"style_id\":\"film_portra\",\"prompt\":\"test\",\"budget_cny_max\":1}"
```

记下返回的 `job_id`，再：

```powershell
curl.exe -s http://127.0.0.1:8000/v1/jobs/把这里换成job_id
```

**验收**：`status` 最终为 `succeeded`（可轮询 2–3 次）；`trace` 里有 renderer 名。

3D 必须 501：

```powershell
curl.exe -s -o nul -w "%{http_code}" -X POST http://127.0.0.1:8000/v1/jobs -H "Content-Type: application/json" -d "{\"modality\":\"asset.3d\",\"style_id\":\"ink_wash\",\"prompt\":\"x\"}"
```

**验收**：打印 `501`。对 `video.long` 再测一次同样 501。

---

## C7. 第一次 Docker 构建（M0 迁徙演练）

确认 `app/api/main.py`、`app_ui.py`（可先空文件或占位）、`scripts/`（可先放一个 `.gitkeep`）、`tests/` 存在，否则按 Dockerfile 的 COPY 补齐。

```powershell
cd F:\item
docker compose build
docker compose up -d
curl.exe -s http://127.0.0.1:8000/v1/health
docker compose down
```

**验收**：容器内 health 与本机 venv 行为一致。  
**未通过**：看 `docker compose logs api`。常见原因：COPY 路径缺文件、lock 在 Linux 装不上（回到 B4 用纯 pip 包、去掉 Windows 特有包后重新 freeze）。

**M0 出门**：venv curl 全过 + Docker health 过。才能进 D。

---

# D. M1 最小闭环（先完成这个再充值生图）

**用户路径（必须亲手走一遍）**：上传 JPG → 说「做成胶片暖调」→ 下载效果图 → 再问「水彩和水墨差在哪」不建 Job。

## D1. 写 10 个 Style Pack YAML

目录：`app/rag/packs/`

艺术 7：`watercolor` `pastoral_anime` `cyberpunk` `ink_wash` `oil_paint` `pixel` `flat_illustration`  
摄影 3：`cinematic_teal_orange` `film_portra` `hk_night`

每个 YAML 含：`style_id` `name` `domain` `positive` `negative`；摄影另含 `photo_look` 参数（exposure/contrast/grain 等）。  
**禁止** 在代码或配方标题写「官方吉卜力授权」；田园风用 `pastoral_anime`。

**验收**

```powershell
Get-ChildItem F:\item\app\rag\packs\*.yaml | Measure-Object
```

Count ≥ 8（建议 10）。

---

## D2. RAG：ingest + query

实现 `app/rag/ingest.py`、`app/rag/retriever.py`，向量库目录 `settings.data_dir / "chroma"`。Embedding：`BAAI/bge-small-zh-v1.5`，设备 `cpu`。

首次 ingest 会下载约 **100–200 MB** 模型（走 `HF_HOME`）。

**验收**

```powershell
curl.exe -s -X POST http://127.0.0.1:8000/v1/styles/ingest
curl.exe -s http://127.0.0.1:8000/v1/styles
curl.exe -s -X POST http://127.0.0.1:8000/v1/rag/query -H "Content-Type: application/json" -d "{\"query\":\"夜景霓虹\",\"k\":3}"
```

- styles 列表 ≥ 8。  
- query 的 Top-3 文档/metadata 含 `cyberpunk` 或 `hk_night`。

---

## D3. 真摄影 Look 引擎（替换 mock）

`app/renderers/photo_look/`：读源图 → 曝光/对比/简单曲线或 LUT → 颗粒 → 写出 JPG。  
输出 **宽高与输入差 ≤ 2 像素**。

准备 1 张自己拍的或有权使用的 JPG，例如 `F:\item\data\eval\sample.jpg`（可先手拷一张）。

**验收**

1. `POST /v1/assets` 上传该图，拿到 `asset_id`。  
2. `POST /v1/jobs`：`modality=image.photo_look`，`style_id=film_portra`，带上该 asset。  
3. Job succeeded，`actual_cost_cny == 0`。  
4. `GET /v1/assets/{out}/file` 能下载；用看图软件打开，颜色有变、人脸还在。

上传示例：

```powershell
curl.exe -s -F "file=@F:\item\data\eval\sample.jpg" http://127.0.0.1:8000/v1/assets
```

---

## D4. LangGraph：Router 两路（qa / render）

节点：ingest → router → (retrieve+plan+photo_look | rag_qa) → 结束。  
工具只允许：`list_styles` `retrieve_style_knowledge` `submit_render_job` `get_job` `estimate_cost`。  
**禁止** 在 node 里 `httpx` 直连接生图厂商。

`.env` 填入 `DEEPSEEK_API_KEY`。若暂时没 Key：实现 `FAKE_LLM=1` 的规则路由（含「胶片」「暖调」→ `film_portra`），**演示给别人看之前必须换成真 LLM**。

**验收**

- 请求 Chat：「水彩和水墨差在哪？」→ 有 `reply`，**无** `job_id`（或 job_id 为空），有 citations。  
- 请求 Chat：上传后的 asset + 「做成胶片暖调，脸别磨皮」→ 有 `job_id`，随后 Job 成功。  
- 日志/trace 能看到 tool 名，而不是厂商 URL。

```powershell
curl.exe -s -X POST http://127.0.0.1:8000/v1/chat -H "Content-Type: application/json" -d "{\"message\":\"水彩和水墨差在哪？\"}"
```

---

## D5. 线程记忆

`thread_id` 可省略（服务端建）或客户端传入；`GET /v1/chat/{thread_id}` 能拉到消息列表和关联 job。

**验收**：同一 thread 连发问答 + 渲染两条，GET 能看到两条消息。

---

## D6. M1 Docker 再打一次包

```powershell
docker compose build
docker compose up -d
# 重复 D3 上传 + Job（注意容器读的是挂载的 ./data）
docker compose down
```

**验收**：容器里 Look Job 仍 0 元成功。  
**M1 出门（8 分钟演示）**：上传 → 胶片 Look → 问答一句 → 打开 trace。过了才能进 E。

---

# E. M2 2D 风格化（开始花钱，先设熔断）

## E0. 充值前检查

`.env` 已设 `PROJECT_BUDGET_CNY=500`、`DAILY_BUDGET_CNY=20`。  
代码在发 HTTP 前读累计消费，超日预算拒绝 `image.2d`。

**验收**：单测或临时把日预算设为 0，提交 2D Job → `BUDGET_EXCEEDED`，**抓包/日志确认没有出网生图请求**。

---

## E1. Provider 适配器

`app/providers/siliconflow.py`（或万相），只许本目录出现厂商 SDK/URL。  
实现文生图、图生图；统一返回本系统 `asset_id`。

**验收**：用 **最低价模型** 各成功 1 张；失败时 Job `failed` 且 `error.code` 可读（鉴权失败、内容审核等）。

---

## E2. `image.2d` Renderer 接入 Planner

Agent：用户说「改成水墨画」→ `modality=image.2d` + `ink_wash`；说「调色/胶片」仍走 `photo_look`。

**验收**：同一张源图，两条路径都有产物；话术选错 modality 算本步未过。

---

## E3. 自动重试最多 1 次

规则示例：输出文件损坏、或人脸检测失败（若已接入）。`retry_count≤1`。

**验收**：人为让第一次 poll 失败，第二次 mock 成功，trace 里 retry=1；再失败则 `failed`，不会死循环。

---

## E4. 7 个艺术风格各出 1 张进入 `data/eval/m2/`

控制花费：开发用 turbo/低价档；每风格 1 张即可。

**验收**：文件夹内 ≥ 5 张（允许先覆盖 5 个风格，报告里写明未覆盖的 style_id）。  
grep 源码无把 Ghibli 官方素材 URL 写死。

---

# F. M3 打磨

## F1. 多轮改稿

同一 thread：「再暗一点」只改 `overrides`（如 exposure/grain），**不改** `style_id`。

**验收**：两次 Job 的 trace JSON diff 仅允许白名单字段变化；style_id 相同。做 5 轮脚本，5/5 通过。

---

## F2. Gradio `app_ui.py`

上传、聊天、展示前后图、显示 job_id 与 `actual_cost_cny`。

```powershell
python app_ui.py
```

**验收**：浏览器能走完 D 段用户路径，不依赖手敲 curl。

---

## F3. 日预算对 UI 仍允许 Look

超日预算：2D 拒绝；`photo_look` 仍成功。

**验收**：把 `DAILY_BUDGET_CNY=0` 测一条 Look 成功、一条 2D 失败，再改回 20。

---

# G. M4 双轨占位

- `app/rag/packs/` 或 `app/rag/docs/` 增加 `shot_grammar`、`material_3d` 各 ≥ 3 篇短文并 ingest。  
- capabilities 里 3D/视频保持 `reserved`。  
- README 或本仓库文档链接设计方案双轨（已有 `设计方案.md` 即可）。

**验收**：`rag/query`「分镜」能命中；`asset.3d` 仍 501。

---

# H. 一期效果评估（没出报告不算结束）

按 `工程选型与一期实施方案.md` §3 执行。最低操作：

1. 建 `tests/eval/golden/`：自有版权图约 12 张 + `golden.json` 标注意图。  
2. 实现 `scripts/eval_report.py`（可先做 E1 路由准确率 + E2 SSIM）。  
3. 人脸可选：

```powershell
python -m pip install mediapipe
python -m pip freeze > requirements.lock.txt
```

4. 运行：

```powershell
python scripts/eval_report.py --golden tests/eval/golden --out data/eval/report.md
```

**出门标准（门禁 + 分数）**

- G1 累计 API ≤ 500 元（用你自己的消费记录 + Job 汇总交叉）  
- G2 3D/长视频黄金负例 100% 拒绝  
- G3 成功 Job 可还原 style_id / renderer / 关键参数  
- G4 无「官方吉卜力授权」话术  
- 总分 ≥ 70，且 Agent ≥ 85%、Look SSIM 均值 ≥ 0.75  

未通过：只修对应维度，不要开新风格或租 GPU。

---

# I. 迁到 Linux 服务器（建议先在本机 Docker 当「假服务器」）

## I1. 本机已等价于服务器

```powershell
cd F:\item
docker compose build
docker compose up -d
curl.exe -s http://127.0.0.1:8000/v1/health
```

**验收**：与 venv 同一套 API。这就是迁徙预演。

## I2. 真正上云时（Ubuntu 为例，在服务器上执行）

不要拷贝 `.venv`。在服务器：

```bash
git clone <你的仓库> styleforge && cd styleforge
nano .env   # 只贴密钥，DATA_DIR 用默认
docker compose up -d --build
curl -s http://127.0.0.1:8000/v1/health
```

数据在 `./data` 卷；防火墙放行 8000（或前面加 nginx）。

**验收**：外网或内网 `health` 200；做一次 Look Job 成功。  
**未通过**：`docker compose logs -f api`；核对 `.env` 没有 Windows 路径。

## I3. 可选：导出镜像拷贝（无 git 时）

```powershell
docker save styleforge-api:phase1 -o F:\cache\styleforge-api-phase1.tar
```

服务器：`docker load -i styleforge-api-phase1.tar` 后改 compose 为 `image: styleforge-api:phase1` 可先不 build。

---

# J. 每天开发的固定顺序（避免环境漂）

```powershell
cd F:\item
.\.venv\Scripts\Activate.ps1
python --version
uvicorn app.api.main:app --reload --host 127.0.0.1 --port 8000
```

改完一个模块：`pytest -q`。  
每个 M 结束：`docker compose build` 抽查一次。  
`pip` 新装包后立刻：`python -m pip freeze > requirements.lock.txt`。

---

# K. 禁止事项（违反就停）

1. 跳过 M1 去调 Flux/万相「先看看效果」。  
2. 本机安装 CUDA / 下载 FLUX 权重做一期。  
3. 把 `.env` 提交 git。  
4. 业务代码写 `F:\item` 或 `C:\Users\...`。  
5. 把 `.venv` 当部署包发给服务器。  
6. 3D/长视频一期写真实付费调用。

---

# L. 卡壳时最小排查

| 现象 | 处理 |
| --- | --- |
| `Activate.ps1` 无法加载 | 回到 A4 |
| `python` 不是 3.11 | 用 `py -3.11` 重建 venv |
| HuggingFace 下载失败 | A6 镜像；或手动把模型放到 `%HF_HOME%\hub` |
| 8000 端口占用 | `netstat -ano | findstr :8000` 结束对应 PID，或换 `STYLEFORGE_PORT` |
| Docker 与 venv 行为不一致 | 以 Docker 为准改代码（那才是服务器） |
| OpenCV 读图中文路径失败 | 资产落盘用 `asset_id` 当文件名，避免中文路径 |

---

按 A → B → C → D 做到 **M1 出门** 后，再打开本文 E 段。做到 D6 之前，一期工程的环境和迁徙包装已经闭环；后面只是往同一套 Job 接口里加 Renderer。
