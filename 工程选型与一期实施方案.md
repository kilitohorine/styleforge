# 工程选型、一期实施方案与效果评估

> 配套文档：`设计方案.md`（产品边界、接口契约、预算硬顶）  
> 本文只回答三件事：**选什么、一期怎么按闭环落地、效果怎么判定过关。**  
> 文档版本：v1.1 · 日期：2026-09-08（对照仓库落地修订）  
> 约束继承：一期成本 ≤ 500 元；不训练；不租包月 GPU；3D / 长视频只留接口。  
> **读法**：§1–§4 仍是选型依据；**与代码不一致处以本节及 §7 为准。**

---

## 0. 怎么读这份文档

| 你想知道 | 去哪一节 |
| --- | --- |
| 为什么是 LangGraph 而不是 CrewAI | §1 |
| 先做哪一块才能演示 | §2.1 最小闭环 M1 |
| 每一块怎样才算做完 | §2.3 分模块验收 |
| **整期是否成功（最重要）** | §3 |
| 电脑要不要显卡、硬盘要多大 | §4 |

数据口径：价格以 2026 年 8 月公开按量价为量级（下单前以控制台为准）；框架热度取公开 GitHub / 招聘提及统计，用于「招聘匹配」和「社区可维护性」，不是唯一技术判据。

---

## 1. 各技术具体选型与依据

原则：**能 API 就不本地跑大模型；能经典算法就不生成；能 SQLite 就不引入中间件。** 每一层只选一个主方案 + 一个备选，避免一期技术栈膨胀。

### 1.1 编排：LangGraph（主） / 不用 CrewAI、AutoGen 做主框架

| 候选 | 公开热度（约 2026-08） | 适合本项目的点 | 不选的原因 |
| --- | --- | --- | --- |
| **LangGraph** | GitHub 约 3.4–4.0 万星；AI 框架提及榜 **LangChain 299 / LangGraph 277**（AI Dev Impact Lab, 2026-08） | 有向图、**Checkpoint**、`interrupt` 人机环、Typed State；和招聘 JD 高度重合 | 文档分散、学习曲线陡——用「小图 + 少节点」对冲 |
| CrewAI | 约 5.3–5.7 万星，PyPI 下载更高 | 角色 Crew 原型快 | 本项目是 **流水线状态机**（检索→规划→渲染→评测），不是「研究员+作家开会」；JD 里远少于 LangChain |
| AutoGen | 星数更高 | 对话式多智能体 | 公开对比中已偏维护态；状态不如 Graph 清晰 |
| 纯 LangChain AgentExecutor | 生态最大 | 入门简单 | 多轮改稿、重试、预算熔断需要显式边，Executor 容易变成不可测黑盒 |
| LlamaIndex | RAG 强 | 知识库 | 本项目 **Agent 优先、RAG 为工具**；检索用 Chroma 即可，不必换编排内核 |

**依据摘要**

- 招聘匹配：同月统计里 LangChain / LangGraph 提及量比 CrewAI（约 20）高一个数量级，作品集用 Graph 更好讲。  
- 架构匹配：设计方案里的 `Router → Retrieve → Plan → Execute → Critique` 就是图，不是角色对聊。  
- 成本：LangGraph 开源可自托管；**不用 LangGraph Cloud**（按 action 计费，Hobby 量级公开测算可到数十美元/月，会吃掉 500 元预算）。

**锁定版本策略**：`langgraph` + `langchain` + `langchain-openai`（兼容 DeepSeek 的 OpenAI 协议）。Pin 在 `requirements.txt`，不追 weekly 主分支。

### 1.2 编排模型：DeepSeek-V4-Flash（主） / 百炼 qwen-plus（备）

| 候选 | 内地公开价（量级） | Tool / JSON | 一期角色 |
| --- | --- | --- | --- |
| **DeepSeek-V4-Flash** | 输入缓存命中 **0.02 元/百万 tokens**，未命中 **1 元**，输出 **2 元**（官网价表） | 支持 Tool Calls、JSON | **默认大脑** |
| DeepSeek-V4-Pro | 未命中输入 3 元、输出 6 元 | 更强推理 | 仅 Planner 失败时升级，默认关闭 |
| 百炼 **qwen-plus** | ≤128K：输入 **0.8 元/百万**，输出非思考 **2 元**（思考模式输出更高，约 8 元档） | 国内备案、生态全 | 备路；同一套 OpenAI 兼容封装切换 `base_url` |
| GPT-4o / Claude | 美元计价，通常高 1–2 个数量级 | 强 | **一期禁用**，500 元顶不住 |

**算一笔账（数据支撑「为什么够用」）**

一次典型渲染对话：输入约 2k tokens（含检索片段）+ 输出约 400 tokens。  
按 Flash **未命中缓存**：  
`2e-3 × 1 + 0.4e-3 × 2 ≈ 0.0028 元/轮`。  
1000 轮编排 ≈ **2.8 元**。500 元预算里 LLM 几乎可以忽略，风险在生图不在对话。

对比：同样 1000 轮若每轮都走 qwen-plus 思考模式，输出单价可到 Flash 的数倍，一期没有必要。

**锁定（落地）**：`llm.chat_model` 使用 OpenAI 兼容的 `deepseek-chat`（环境变量 `DEEPSEEK_MODEL`，空 Key 则走关键词路由、0 元）。选型时的「V4-Flash」是价表口径，不以营销名写死在代码里。`temperature=0` 用于 Router 结构化输出。Critique 不调用 LLM，用 SSIM。

### 1.3 视觉理解（可选、默认关）：只用于评测抽样

| 候选 | 用途 | 一期策略 |
| --- | --- | --- |
| 百炼 qwen-vl / 硅基流动 GLM-5V 等 | 「这张还像不像水墨」「脸糊没糊」 | **默认关**；评测集抽样 20 张再开，避免每张图都打 VLM |
| 本地 CLIP ViT-B/32 | 文本-图像方向相似度 | **一期未装**；E3 真出图后再作为评测机可选依赖 |
| 本地 InsightFace | 人脸余弦 | **未装**；感知人脸用 OpenCV Haar |
| 本地 MediaPipe | 人脸 | **未装**（避免额外权重）；M1 用 Haar 即可 |

依据：VLM 按次/按 token 计费，若每张图都调用，会把「评测」变成最大开销。设计方案已把 VLM 预算单列为约 30 元——只够抽样，不够全量。

### 1.4 2D 生成：硅基流动（主） / 阿里万相（备） / ComfyUI（接口预留、一期不装模型）

| 候选 | 公开单价量级 | 图生图 | 一期 |
| --- | --- | --- | --- |
| **硅基流动** FLUX.1-dev / Kontext / Z-Image-Turbo | Turbo 约 **$0.005/张**；FLUX.1-dev 约 **$0.014/张**；Kontext Pro 约 **$0.04/张**（平台标价，折人民币随汇率） | Kontext 适合「保留内容改风格」 | **主：日常用低价档，作品集用 Kontext** |
| **阿里万相** wanx 文生图 | **0.20 元/张**（公开表） | 有图生图产品线 | **备：国内账单、内容审核更稳** |
| 本地 FLUX.1-dev | 电费 + 磁盘 | 最强可控 | **一期不部署**：FP16 权重约 **23.8 GB**，舒适推理要 **24 GB 级显存**；FP8 单文件约 **17 GB**，峰值仍可贴满 4090。与「不训练、≤500、先做 Agent」冲突 |
| Replicate / fal.ai | 与 BFL 接近（Kontext Pro 约 $0.04） | 好用 | 国际支付与延迟，不作默认 |

**220 元生图预算能买到什么（按设计方案分配）**

- 若均价 0.05 元/张 → 约 **4400 张**（调 prompt、回归）  
- 若均价 0.20 元/张（万相）→ 约 **1100 张**  
- 若均价 0.30 元/张（Kontext 编辑折算）→ 约 **700 张**  

策略：**开发用低价模型；验收黄金集用固定档位**，避免「今天 Turbo 明天 Max」导致评测不可复现。

**锁定（落地）**：`providers.image.primary=siliconflow`，`fallback=dashscope_wanxiang`（`IMAGE_2D_BACKEND` 切换）。厂商 URL 只出现在 `app/providers/`。无 Key **失败关闭**，不发 HTTP。万相适配器已接线，live 出图仍需额度。

### 1.5 摄影 Look：OpenCV + Pillow + 3D LUT（主） / 生成模型（辅）

| 候选 | 成本 | 保真（脸/构图） | 像不像 Pr |
| --- | --- | --- | --- |
| **LUT + 曲线 + 颗粒**（OpenCV `cvtColor` / 3D LUT 插值，Pillow 颗粒） | **0 元/张** | 像素网格不变，只改颜色，SSIM 通常很高 | **最像 Lumetri** |
| 仅 img2img 电影感 prompt | 0.01–0.30 元/张 | 易改五官、易改构图 | 更像「滤镜生成」 |
| 商用调色 SDK | 授权费 | 稳 | 超预算、无 Agent 训练价值 |

依据：Premiere 相片/成片气质的主路径是 **Look 而不是重绘**。一期「替代 Pr」的承诺范围是 Lumetri 段（见设计方案 §0.2），因此 Look 引擎必须是独立 Renderer，不能做生成模型的副作用。

**锁定**：`image.photo_look` 默认 `photo_look.cube_lut`（OpenCV 插值自烘焙 `.cube`）；仅当用户明确说「改成油画/像素」时才串联 `image.2d`。

### 1.6 RAG：Chroma（主） + BGE-small-zh（主） / BGE-M3（可选升级）

**向量库**

| 候选 | 热度（同月提及） | 运维 | 一期数据量 |
| --- | --- | --- | --- |
| **Chroma** | 向量库提及 **176**，高于 pgvector 49、Pinecone 24 | 内嵌、零服务 | Style Pack + 手册 ≪ 1 万 chunk |
| FAISS | 学术常用 | 要自己做持久化与元数据 | 元数据过滤（`style_id`/`modality`）不如 Chroma 省事 |
| Qdrant / Pinecone | 生产强 | 进程或云费 | 杀鸡用牛刀 |
| pgvector | 以后和业务库一体 | 要装 Postgres | 一期禁止引入数据库运维 |

本项目检索集：约 **15** 个 YAML × 若干手册 × 占位文档，**总文本 < 2 MB**。HNSW 内存可忽略（公开经验：百万向量才到 GB 级；我们是百级向量）。

**Embedding**

| 模型 | 体积 | 能力数据 | 一期 |
| --- | --- | --- | --- |
| **n-gram hash 384d** | 0 下载 | 中文 n-gram + token hashing，知识库极小 | **一期默认** |
| BAAI/bge-small-zh-v1.5 | 约 **100 MB** 量级，CPU 友好 | C-MTEB 检索约 **61.8**（small）vs large 约 **70.5** | 可选升级，不进默认 requirements |
| BAAI/bge-m3 | 文档标称 **569M 参数 / 约 2.27 GB** | 多语、8k 上下文、密疏多向量 | 知识库变大或中英混检再升级 |
| OpenAI text-embedding-3 | 按 token 钱 | 强 | 不必要，且密钥与出境 |

**锁定（落地）**：默认 `sentence-transformers` **不进** `requirements.txt`，避免拉取 torch。检索用 **n-gram hash 384 维**（0 下载，确定性）。BGE-small-zh 仍为知识库显著变大时的可选升级，不作为一期出门条件。Chunk：按 YAML 字段切（positive / negative / 适用场景）。

### 1.7 API 与契约：FastAPI + Pydantic v2

| 候选 | 依据 | 结论 |
| --- | --- | --- |
| **FastAPI** | 异步原生；Pydantic 生成 OpenAPI；和 LangGraph 同属 Python 一等公民 | **主** |
| Flask | 同步为主，OpenAPI 要插件 | 不选 |
| Django | 重，Admin 一期用不到 | 不选 |
| 纯 Gradio 当后端 | 演示快 | 接口不稳定，3D/视频无法复用契约 |

**锁定**：所有对外 DTO 与 `RenderJob` 共用 Pydantic 模型；`GET /openapi.json` 作为接口验收物。

### 1.8 任务与存储：SQLite WAL + 本地磁盘（主） / Redis+ARQ（二期）

一期并发：个人演示，**QPS ≪ 1**。SQLite 官方文档量级可支撑远高于此的读；Job 表一行几 KB，一万任务仍是 MB 级。

不选 Redis 的依据：多一个必须常驻的服务，Windows 开发体验差，且 500 元方案强调「本机就能跑」。

**锁定**：`jobs.db` + `data/assets/{asset_id}`；状态机见设计方案 §6.4。FastAPI `BackgroundTasks` 先跑通；若出现「请求线程被生图 HTTP 阻塞」，再抽 `worker.py` 独立进程，仍写同一 SQLite。

### 1.9 前端：Gradio（主） / 静态 HTML（备）

| 候选 | 依据 |
| --- | --- |
| **Gradio** | 上传图 + Chat + 轮询 Job，约一个 `app_ui.py`；Hugging Face 作品集常见 |
| Streamlit | 刷新模型偏整页，聊天+轮询稍别扭 |
| React | 一期禁止为 UI 开仓库 |

### 1.10 观测：结构化日志（主） / LangSmith 免费档（可选）

LangSmith 对讲「Agent tracing」有招聘加分，但不是正确性来源。一期：每 Job 写 `trace.json`（style_id、prompt、cost、tool 序列）。可选开 LangSmith，超免费额度即关。

### 1.11 测试：pytest + 黄金集（主） / 不要一期上复杂仿真

Agent 单测：对 LLM 使用 **fake 结构化输出**（固定 JSON），对 Renderer 使用 **mock**。真 API 只在 `tests/live` 且默认 skip，避免 CI 烧钱。

### 1.12 选型总表（一期冻结）

| 层 | 主选 | 备选 | 明确不做 |
| --- | --- | --- | --- |
| Agent | LangGraph 0.3.x | 无 | CrewAI 主框架、LangGraph Cloud |
| LLM | DeepSeek `deepseek-chat`（可空 Key） | 百炼 qwen-plus（未接线） | GPT-4o 默认 |
| 2D | 硅基流动 | 万相（适配器已加，无 Key 关闭） | 本地 FLUX 权重 |
| Look | OpenCV + 自烘焙 `.cube`（**8** 个） | 串联 2D | 商用调色套件 |
| RAG | Chroma + n-gram hash | bge-small-zh | Pinecone |
| API | FastAPI | — | Django |
| Job | SQLite WAL | ARQ | K8s / Celery 集群 |
| UI | Gradio | HTML | 完整 NLE |
| 评测 | 规则 + 自实现 SSIM + `eval_report.py` | 抽样 VLM | 每张都打商业 VLM；默认 CLIP |

---

## 2. 一期具体工程方案

节奏：**先最小闭环可演示，再补会烧钱的生图，最后才是体验打磨。** 与设计方案 Phase 0–4 对齐，但把「能给别人点开看」提前到 M1。

### 2.1 里程碑总览

```
M0 骨架（约 2 天）     接口 + Job + mock 渲染，3D/视频 501
        ↓
M1 最小闭环（约 4 天）  ★先完成这个再写生图
        Chat + RAG + 摄影 Look（落地为 **8** 个 cube）+ 上传下载 + 成本记账(0元)
        ↓
M2 核心增强（约 4 天）  2D Provider + 7 艺术风格配方 + 有限重试（真出图依赖 Key）
        ↓
M3 打磨（约 2 天）      多轮改稿、Gradio、黄金集 **mock** 回归、日预算熔断
        ↓
M4 双轨占位（约 0.5 天） schema 冻结，知识库放入分镜/材质占位文
```

后续功能（**一期不做实现，只保持可插拔**）：3D Provider、视频秒级生成、LoRA、批量 20 张流水线、EDL 导出。

### 2.2 最小闭环（M1）定义——必须先完成

用户路径（**零生图费用**）：

1. 启动 `uvicorn` + 可选 Gradio  
2. 上传一张人像或夜景 JPG  
3. 说：「做成胶片暖调，脸别磨皮」  
4. Agent 检索到 `film_portra`，提交 `image.photo_look` Job  
5. 30 秒内可下载 **原图 | 效果图 | 参数 JSON**  
6. 再问：「水彩和水墨差在哪？」只走 RAG，不创建 Job  

若这条不通，禁止进入 M2（否则钱会花在调不通的图上）。

### 2.3 分阶段任务、优先序与验收标准

#### M0 — 技术骨架

| 任务 | 完成定义 | 验收 |
| --- | --- | --- |
| Pydantic 信封与 OpenAPI | 设计方案 §6 路径全部注册 | `GET /openapi.json` 含 `jobs/chat/styles/assets/capabilities` |
| Job 状态机 | queued→running→succeeded/failed | 单测：非法跳转失败 |
| Renderer Protocol | mock 返回一张纯色图 | `POST /v1/jobs` `image.photo_look` 能入队出队 |
| 模态预留 | 3D/视频 | `modality=asset.3d` 或 `video.long` → **HTTP 501** + `MODALITY_RESERVED`，且 body 通过 schema 校验 |
| 密钥 | `.env.example` 无真实 key | git 不含 `.env` |
| 预算字段 | `budget_cny_max` | mock 单价为 0 时仍写入 `estimated_cost_cny` |

**M0 出门标准**：用 curl/httpie 走完上传→创建 Job→GET 状态，不依赖任何付费 API。

#### M1 — 最小闭环（核心功能优先）

| 功能（对应设计 C） | 工程内容 | 验收标准 |
| --- | --- | --- |
| C6 资产 | 本地落盘、id 返回 | 上传 8MB JPG 成功；非法类型 415 |
| C2 RAG | Style Pack 入库（艺术 7 + 摄影 **8** = 15）+ 分镜/材质占位文 | `GET /v1/styles` 返回 ≥15 条；`POST /v1/rag/query` 对「夜景霓虹」Top-3 含 `cyberpunk` 或 `hk_night` |
| C1 Router | LangGraph：`route → perceive → execute → critique`；问答在 route 后结束 | 问风格差异 **不** 调 renderer；渲染话术 **会** 调 Look 或 2D |
| C4 Look | **8** 个摄影 Look 可运行 | 输出尺寸与输入一致（边长差 ≤ 2px）；CI 用几何色块，禁止把合成网图当作品集 |
| C8 问答 | citation | 回答含 `style:` 或 pack id |
| C9 成本 | Look=0 | 成功 Job `actual_cost_cny==0` |
| 可追溯 | `trace` | 含 `style_id`、`renderer=photo_look.cube_lut`、LUT 文件名 |

**M1 出门标准（演示脚本 8 分钟）**：上传→胶片 Look→问答一条→打开 `trace`。失败则不开 M2 API 充值。

#### M2 — 2D 风格化（核心功能的「生成」部分）

| 功能 | 工程内容 | 验收标准 |
| --- | --- | --- |
| C3 Provider | 至少 1 个真后端（硅基流动或万相） | 文生图、图生图各 1 条成功；失败映射为 `failed` + 可读 `error.code` |
| 双后端开关 | 环境变量 | 切断主 key 时备路可切换（允许手动改 env 复测） |
| 风格外置 | 禁止硬编码吉卜力商标 prompt | grep 代码无 `ghibli` 厂商 prompt；仅 `pastoral_anime` |
| C7 重试 | 规则：人脸检测失败或文件损坏 | 最多 **1** 次自动重试；`retry_count≤1` |
| 预算熔断 | 单价表 | `budget_cny_max=0.01` 且模型单价更高 → **不发请求**，`BUDGET_EXCEEDED` |
| 艺术风格 | 7 个 Pack 能出图 | **代码与 mock 已通**；每风格黄金集实图需 Key，写入 `tests/eval/m2/`（当前 README 占位，无实图） |

**M2 出门标准**：同一张源图，`photo_look` 与 `image.2d` 两条路径都有产物；Agent 能根据话术选对 modality（「调色」vs「改成水墨画」）。

#### M3 — 打磨

| 功能 | 验收标准 |
| --- | --- |
| C5 多轮 | 同一 `thread_id`：「再暗一点」只改 param，不丢 `style_id`；Look 路径对比两次 JSON 的 diff 仅限允许字段 |
| Gradio | 上传、聊天、展示前后图、显示 job_id 与费用 |
| 日预算 | `DAILY_BUDGET_CNY` 超限拒绝生图，Look 仍可用 |
| 回归 | `pytest tests/eval -m golden` 在 mock 下全绿；`tests/live` 默认 skip（需 `STYLEFORGE_LIVE=1`） |

#### M4 — 后续功能占位（一期最后做，工作量小但必须做）

| 项 | 验收 |
| --- | --- |
| `shot_grammar` / `material_3d` 各 ≥3 篇入库 | `rag/query` 能命中 |
| capabilities | `asset.3d` / `video.*` 状态为 `reserved` |
| README 指向设计方案双轨 | 招聘口述不翻车 |

### 2.4 建议目录与先写的文件（落地顺序）

```
styleforge/
  app/schemas/job.py          # 第 1 个文件：冻结信封
  app/renderers/base.py
  app/api/main.py
  app/jobs/store.py
  app/rag/packs/*.yaml        # 15 个配方（8 Look + 7 艺术）
  app/graphs/agent.py         # route / perceive / execute / critique
  app/providers/siliconflow.py
  app/providers/dashscope.py  # 万相备路
  tests/eval/golden/          # mock 黄金集
  scripts/eval_report.py      # §3 评分（E3 live 可 skip）
```

### 2.5 一期明确推迟的功能（避免范围膨胀）

| 推迟项 | 原因 | 何时做 |
| --- | --- | --- |
| 本地 ComfyUI + FLUX | 17–35 GB 磁盘 + 高显存 | 有 24GB 卡且 M3 已过 |
| LoRA 训练 | 设计方案 §9 | 黄金集证明某风格稳定失败 |
| 视频生成 API | 0.6–1 元/秒，一条 5 秒可到 3–5 元 | 另批预算 |
| 3D mesh API | 单次美元级 | 另批预算 |
| 用户体系 / 云 OSS | 与学习目标无关 | 作品集不需要 |

---

## 3. 最终效果如何评估（一期最重要的一节）

工程验收（§2.3）只能证明「能跑」。**最终效果**要回答四句话：

1. Agent 是否选对工具和风格？（编排质量）  
2. 摄影 Look 是否像调色而不是毁图？（替代 Pr 的承诺）  
3. 2D 风格化是否看得出目标风格、内容还在？（生成质量）  
4. 是否在 500 元内可复现？（约束）

四条 **全部达标** 才算一期成功。只出几张好看的图但 Agent 乱调工具，或 Look 把脸扭曲，都算失败。

### 3.1 黄金集（固定、可回归）

建 `tests/eval/golden/`。CI **禁止把 AI 生成图当源**（避免循环自测）。几何色块仅用于自动 SSIM/尺寸。作品集人像/风光须自有版权，当前未入库。

| 子集 | 数量 | 内容 |
| --- | --- | --- |
| P 摄影源 | 12 | 4 人像（正/侧/逆光/夜景）、4 风光、2 街拍、2 静物；自有版权或明确可发布 |
| A 艺术意图 | 10 条固定话术 | 覆盖 7 艺术风格 + 3 易混（水彩 vs 水墨、赛博 vs 港风） |
| L Look 意图 | 8 条 | 「胶片暖调」「电影青橙」「港风夜景」及改稿句 |
| Q 问答 | 10 条 | 无图，只测 RAG |
| N 负例 | 8 条 | 「生成 3D 模型」「剪 10 分钟视频」「吉卜力官方同人训练」→ 应 501 或拒答并说明 |

源图分辨率统一最长边 1280，JPEG 质量 90，便于对比。

### 3.2 评分维度与权重

总分 100。**低于 70 或任一门禁未过 → 一期未通过。**

| 门禁（任一失败则总分无效） | 标准 |
| --- | --- |
| G1 预算 | 项目累计 API ≤ 500 元；日志可加总 |
| G2 契约 | 黄金集负例 3D/长视频 100% 501 或明确拒绝，不调用付费视频/3D |
| G3 可复现 | 任意成功 Job 能还原 style_id / renderer / 关键 params |
| G4 版权话术 | 产品层不出现「官方吉卜力授权」 |

| 维度 | 权重 | 测什么 | 方法 |
| --- | --- | --- | --- |
| E1 Agent 正确性 | 25 | 路由、工具、风格 id | 自动 |
| E2 摄影 Look | 25 | 结构保留 + Look 方向 | 自动 + 人看 |
| E3 2D 风格化 | 25 | 风格可识别 + 图生图保内容 | 自动 + 人看 |
| E4 RAG 问答 | 10 | 命中与引用 | 自动 |
| E5 体验与工程 | 15 | 时延、失败可读、多轮 | 半自动 |

### 3.3 E1 Agent 正确性（自动，招聘时最能讲）

在 **fake LLM 或录制 fixture** 上跑 30 条标注对话（输入 → 期望 `intent` + `style_id` + `modality` + 是否调用 render）。

| 指标 | 计算 | 达标 |
| --- | --- | --- |
| 意图准确率 | 对的 intent / 30 | ≥ 90% |
| 风格 Top-1 | 渲染类里 style_id 正确 | ≥ 85% |
| 模态准确率 | photo 话术不走 2D；「改成水墨画」走 2D | ≥ 90% |
| 工具合法率 | 无「直接调厂商 HTTP」；仅注册 tools | 100% |
| 问答不渲染率 | Q 子集创建 Job 数为 0 | 100% |

真人 LLM 抽测 10 条，允许与 fixture 差 1 条风格近义（如 `hk_night` vs `cyberpunk` 夜景）——近义需在 `alias.yml` 里预先声明，否则算错。

### 3.4 E2 摄影 Look（替代 Pr 是否成立）

对 P×3 个 Look = 36 张输出。

**自动（门禁 + 分数）**

| 指标 | 含义 | 达标 |
| --- | --- | --- |
| 尺寸一致 | H、W 与输入差 ≤ 2 | 100% 张 |
| 结构相似 SSIM | 灰度 SSIM（或 Sobel 边缘图 SSIM） | 均值 ≥ **0.75**（Look 不应重绘） |
| 人脸存在 | MediaPipe/Haar：输入有脸则输出有脸 | ≥ 95%（无脸的风光图跳过） |
| Look 方向 | 见下表启发式 | ≥ 80% 张命中对应规则 |

启发式（写进 `scripts/eval_look.py`，阈值可校准一次后冻结）：

- `cinematic_teal_orange`：阴影 a/b 通道相对原图更偏青、高光更偏橙（Lab 分位差符号正确）  
- `film_portra`：整体更暖（平均 b 通道上升）且饱和度不过度（ΔC 有上限，防止「假胶片过饱和」）  
- `hk_night`：暗部压低、高光色偏品红/青之一（与配方一致）

**人工（12 张抽样，1–5 分）**

- 像调色台还是像滤镜 APP 乱涂：≥ 4 算过  
- 皮肤是否塑料：人像必须 ≥ 4  

人工均值 ≥ 3.7 且无人像 < 3。

### 3.5 E3 2D 风格化

每艺术风格 1 条文生图 + 1 条图生图（源来自 P 子集），共 14 张核心 + 允许 6 张加试，**计入成本账**。

| 指标 | 方法 | 达标 |
| --- | --- | --- |
| 风格可识别 | 三人盲选：5 选 1 风格标签（含干扰项） | Top-1 准确率 ≥ **70%** |
| 图生图内容 | CLIP 图像余弦（源 vs 出）或 DINO；人脸图加「有脸」 | 余弦均值 ≥ **0.70** 或人脸保留率 ≥ 80%（二选一主报告，阈值在试跑 5 张后冻结） |
| 明显崩坏 | 纯色、全黑、全噪、安全策略空图 | ≤ 1 张；出现则必须走 C7 重试记录 |
| CLIP 方向（辅助，不单独否决） | `clip_out · (clip_style - clip_src)` 相对随机风格更高 | 作为分析图，写入报告，**不设一票否决**（对水墨等抽象风格不稳） |

说明：生成质量主观性强，**盲选 70%** 是一期主指标；CLIP 只防完全跑题。不要用 FID（需要大量图，一期没有）。

### 3.6 E4 RAG 问答

10 条 Q：人工标「应引用的 style_id 或文档 id」。

| 指标 | 达标 |
| --- | --- |
| Recall@3 含正确文档 | ≥ 80% |
| 回答含 citation 字段 | 100% |
| 无编造不存在的 style_id | 100% |

### 3.7 E5 体验与工程

| 指标 | 达标 |
| --- | --- |
| Look Job P95 | 本机 ≤ **8 s**（8MP 以内；超大图先缩放到最长边 2048） |
| 2D Job | 只记录，不设死线（取决于厂商，目标 P95 ≤ 60 s） |
| 失败信息 | 无裸 stack 给用户；有 `error.code` |
| 多轮 | M3 脚本 5 轮补丁，style_id 不变 5/5 |

### 3.8 综合判定规则

```
if not (G1 and G2 and G3 and G4):
    结果 = 未通过
else:
    总分 = 25*E1 + 25*E2 + 25*E3 + 10*E4 + 15*E5   # 各维先归一到 0–1
    通过 = 总分 >= 70 且 E1>=0.85 且 E2>=0.75
```

E3 若因预算只跑了 7 风格中的 5 个：允许总分达标，但报告必须写明「未覆盖风格列表」，作品集不得声称 10 风格全优。

### 3.9 评测怎么跑（工程化）

```
python scripts/eval_report.py --golden tests/eval/golden --out data/eval/report.md
```

报告必须包含：总分、门禁、每维表、**累计人民币**、随机种子、模型名、日期。  
这就是招聘时「我们有评测闭环」的证物，比多做两个风格重要。

### 3.10 效果评估里不要做的事

- 不要用「我觉得很吉卜力」当唯一验收。  
- 不要为刷 CLIP 去堆 prompt 长度。  
- 不要把 VLM 当裁判打全体 14 张（贵且不稳）；VLM 只解释争议样本。  
- 不要把二期 3D/视频清晰度写进一期通过条件。

---

## 4. 技术工具、环境与硬件（含存储）

### 4.1 软件工具清单

| 类别 | 工具 | 用途 |
| --- | --- | --- |
| 语言 | Python **3.12**（3.10–3.12 可；本机锁定 3.12。不要用 3.14） | 运行时 |
| 包管理 | `venv` + `pip`；可选 `uv` | 可复现 |
| 核心库 | `langgraph` `langchain` `langchain-openai` | Agent |
| | `fastapi` `uvicorn` `pydantic` `python-multipart` | API |
| | `chromadb` | RAG（默认 hash embedding，无 torch） |
| | `opencv-python-headless` `pillow` `numpy` | Look |
| | `httpx` | Provider |
| | `pytest` `pytest-asyncio` | 测试 |
| | `gradio` | 演示 UI |
| 评测 | 自实现 SSIM（`app/graphs/critique.py`）、Haar 人脸；**不默认** mediapipe / open-clip | §3 mock 报告 |
| 系统 | **Git**、**ffmpeg**（一期 Look 不必须；视频轨预装以免二期卡环境） | |
| 账号 | DeepSeek API；硅基流动 **或** 阿里云百炼（万相+可选 VL） | 付费能力 |
| IDE | Cursor / VS Code | — |
| 可选 | LangSmith 账号（免费档） | 追踪 |

**不要装**：CUDA 全家桶、ComfyUI、PyTorch CUDA 版（除非你已经为 CLIP 评测需要 CPU/GPU 版 torch——`sentence-transformers` 会拉 CPU torch，约 200–400 MB 额外，可接受）。

### 4.2 运行环境

| 项 | 要求 |
| --- | --- |
| OS | Windows 10/11（当前环境）或 macOS/Linux，一期以 Win 为准 |
| 网络 | 能访问 DeepSeek + 所选生图 API；公司代理需配置 `HTTP(S)_PROXY` |
| 环境变量 | `DEEPSEEK_API_KEY` `SILICONFLOW_API_KEY` 或 `DASHSCOPE_API_KEY` `IMAGE_2D_BACKEND` `PROJECT_BUDGET_CNY=500` `DAILY_BUDGET_CNY=20` |
| 工作目录 | 仓库 + `data/`（可 gitignore 大文件） |
| 启动 | `uvicorn app.api.main:app --reload --port 8000`；UI `python app_ui.py` |

国内 Hugging Face 下载 embedding：可用 ModelScope 镜像或预先拷贝 `~/.cache/huggingface`。写入 README 的一条命令即可。

### 4.3 硬件需求（一期默认：无 GPU）

一期路径是 **CPU + 云 API**。显卡 **不是门槛**。

| 配置 | CPU | 内存 | 显卡 | 能否完成一期 |
| --- | --- | --- | --- | --- |
| **最低** | 4 核（如 i5 8 代 / R5 3000） | **8 GB** | 核显即可 | 能，Chroma+OpenCV 紧；关其它浏览器标签 |
| **推荐** | 6 核以上 | **16 GB** | 任意 | 舒适跑 Gradio + 评测 |
| 更好 | 不限 | 32 GB | 有也行 | 只对 M3 CLIP 评测加速，仍不跑 FLUX |

| 组件 | 内存占用量级 |
| --- | --- |
| Python + FastAPI | 150–300 MB |
| Chroma + hash embedder | < 100 MB |
| OpenCV 处理 20MP 照片 | 峰值约 数 100 MB–1 GB（先缩放到 2048 边则更稳） |
| Gradio | 200–400 MB |
| **推荐常驻合计** | **约 1.5–2.5 GB**（不再默认加载 BGE/torch），8 GB 机器可跑、16 GB 安心 |

**显卡**

- 一期：**0 GB VRAM 需求**。  
- 若安装 CLIP 评测且有 NVIDIA：可设 `cuda`，非必须。  
- **不要**为了一期去买卡或租 4090。本地 FLUX：权重 17–24 GB + 显存 12–24 GB，属于二期可选。

### 4.4 存储空间（请按此预留磁盘）

单位：十进制 GB 约数。路径建议：`data/` 与 Hugging Face cache 放同一块空盘。

#### A. 一期「推荐配置」——必须预留

| 用途 | 大小 | 说明 |
| --- | --- | --- |
| 源码与 git | 0.05 | 不含模型 |
| Python venv（含 torch CPU + cv2 + chroma） | **2.5–4.5** | 最大头 |
| Embedding `bge-small-zh-v1.5` | **0**（默认不装） | 可选升级才占用 0.1–0.2 GB |
| 可选 CLIP 评测权重 | **0.4–1.5** | 仅评测机 |
| Chroma 持久化 | < 0.05 | 知识库极小 |
| Style Pack + LUT（`.cube` 三个～十个） | 0.02–0.1 | 3D LUT 单个常 < 5 MB |
| 黄金集源图 12 张 | 0.03–0.08 | |
| 用户资产 + 输出（按 200 张 JPG 计，单张 3–8 MB） | **1–2** | |
| 2D 生成缓存 300 张 | **1–2** | |
| SQLite / 日志 / trace | 0.05–0.2 | |
| **一期小计** | **约 8–12 GB** | |
| **建议空闲** | **20 GB** | 含 Windows 更新与失败重下依赖 |

#### B. 不要在一期下载的（避免磁盘爆炸）

| 项 | 体积 | 何时才需要 |
| --- | --- | --- |
| FLUX.1-dev FP16 | ~23.8 GB + VAE/文本编码器，合计常 **30–40 GB** | 本地 ComfyUI |
| FLUX FP8 一体 | ~17 GB | 同上 |
| BGE-M3 | ~2.3 GB | 知识库显著变大 |
| InsightFace 大模型包 | 0.3–1 GB | 人脸验收不够用时 |
| 视频样片 | 每分钟 1080p 约 0.1–0.5 GB | 轨 2 |

#### C. 磁盘规划建议

```
C:  系统 + venv          预留 30 GB 空闲（Win 本身会吃）
D:  data/assets          一期 10 GB 配额够用
    data/eval
    .cache/huggingface   若 C 盘小，设 HF_HOME 到 D
```

Windows 若 C 盘 < 40 GB 空闲：venv 和 HF cache **必须**改到其它盘，否则安装 `sentence-transformers` 失败概率高。

### 4.5 网络与密钥以外的「环境」

- 系统时间准确（否则 JWT/签名类 API 偶发失败）。  
- 照片测试集：**不要**用未授权的电影截图、游戏壁纸当黄金集。  
- 电源：Look 可笔记本电池；跑 live 生图时接电源，避免休眠中断 Job。

### 4.6 一句话硬件结论

> **一期：16 GB 内存、20 GB 磁盘空闲、稳定宽带、无独显即可。**  
> 把钱花在 API 额度而不是显卡上。等 M3 评测通过、真正要本地高可控生图时，再考虑 24 GB 显存机器或按量租卡，且仍不进入训练。

---

## 5. 执行清单（按周）

| 时间 | 目标 | 花费 |
| --- | --- | --- |
| 第 1 段 | M0 + M1 | ≈ 0 元（DeepSeek 可先用赠额） |
| 第 2 段 | M1 演示给自己验收 §2.2 | ≈ 0–5 元 |
| 第 3 段 | M2 生图 + 黄金集 E3 | 主烧 **生图 150–250 元** |
| 第 4 段 | M3 + `eval_report.py` 出报告 | 余量 50–100 元做重试 |
| 结束 | §3 门禁（mock）+ 代码冻结 | 累计 API **允许为 0**（无 Key 时 Look 闭环仍算工程完成） |

**评测报告**：`python scripts/eval_report.py` 必须能出 mock 报告。无 Key 时 **E3=skipped**，总分会因 E3 权重被拉低——这是诚实结果，**不得改分充过**。有额度后再补 E3 与版权源图。

工程一期（能演示、能回归、能写简历）以 §7 勾选为准；§3 百分制「全部达标」在缺 Key / 缺版权图时 **标记为部分完成**。

---

## 6. 与设计方案的对照

| 设计方案 | 本文落地 |
| --- | --- |
| 不训练、≤500 | §1.4 不下载 FLUX；§3 G1 |
| 先 Look 再生图 | §2.1 M1→M2 |
| 统一 Job / 双轨 stub | M0 / M4 验收 |
| 10 风格 | M1 入库 **15 Pack**（8 Look 可渲染 + 7 艺术配方）；E3 允许未出图但须声明 |
| 招聘向 Agent | E1 权重大、必须 ≥ 0.85 |

下一份可执行物不是再写文档，而是按 §2.4 **先提交 `app/schemas/job.py` 与 OpenAPI**。

---

## 7. 落地偏差与一期实际完成（2026-09-08）

对照原 v1.0 计划，**已改文档锁定、以仓库为准**的差异如下。

| 原计划 | 实际落地 | 原因 |
| --- | --- | --- |
| 摄影 Look × 3 | **8** 个自烘焙 `.cube` | 一期扩风格，仍 0 元/张 |
| Style Pack × 10 | **15** YAML（8+7） | 与 Look 一一对应 |
| 图：`Retrieve → Plan → Execute → Critique` | `route → perceive → execute → critique` | QA 检索折进 `route`；节点名避开 LangGraph 与 state key 冲突（不能叫 `perceive`/`critique`） |
| BGE-small-zh + sentence-transformers | n-gram hash 384d | 不下载 HF、不拉 torch，8GB 机更稳 |
| DeepSeek-V4-Flash 写死 | `deepseek-chat`，Key 可空 | 跟控制台模型名；无 Key 关键词路由 |
| Critique 用 LLM temperature 0.2 | 亮度 SSIM + 尺寸，最多再 execute 1 次 | 0 元、可单测 |
| MediaPipe / CLIP 默认评测 | Haar + 自实现 SSIM；CLIP 未装 | 控制依赖体积 |
| Python 3.11 | **3.12**（禁止 3.14） | 本机解释器 |
| Job 同步即可 | SQLite **WAL** + `queued→running→succeeded/failed` 非法跳转拒绝 | 补 M0 状态机 |
| 万相仅环境变量构想 | `app/providers/dashscope.py` 已加 | 无 Key 失败关闭，与硅基流动对称 |
| `eval_report.py` 未写 | `scripts/eval_report.py` + `pytest -m golden` | mock 黄金集 30 条路由 + Look 几何色块 |
| 12 张版权黄金摄影 | **未入库** | 须你自己的可发布照片 |
| 7 艺术风格作品集实图 | **未提交**（`tests/eval/m2/README.md`） | 需生图 Key 与额度 |
| MCTS / GRPO / VLM 抽样 | 开关默认关，未实现搜索 | 超出一期预算与范围 |
| 3D / 长视频 Provider | HTTP **501** `MODALITY_RESERVED` | 一期只留接口 |
| LangSmith | 未接 | Job `trace` 为招聘证物 |
| LangGraph Cloud | 未用 | 吃预算 |

### 7.1 已完成（可写进简历的工程事实）

- [x] M0 骨架：OpenAPI 含 jobs/chat/styles/assets/capabilities；3D/视频 501；`.env` 不入库
- [x] M1：上传 → 8 Look LUT → RAG 问答 citation → cost=0
- [x] M2 代码路径：硅基流动 + 万相备路 + 预算熔断 + 1 次可重试；无 Key 关闭
- [x] M3：多轮 param patch、Gradio、日/项目预算、Docker Compose
- [x] M4：`shot_grammar` / `material_3d` 入库；capabilities `reserved`
- [x] mock 评测报告脚本与 `pytest -m golden`

### 7.2 明确未完成（简历禁止写成已交付）

- [ ] 自有版权 12 张替换 `samples/` / 黄金源图
- [ ] 7 风格 live 出图进作品集
- [ ] E3 盲选 70%、CLIP 方向、VLM 抽样
- [ ] MCTS 子图、UGC Reward（GRPO）
- [ ] `asset.3d` / `video.*` 真 Provider

简历写法见仓库根目录 [`简历写法.md`](./简历写法.md)。

