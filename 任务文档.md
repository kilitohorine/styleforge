# 任务文档

> 对应仓库当前版本：一期工程收口（8 Look + perceive/critique + Docker + RAG/2D 预算 + mock 评测 + 万相备路）  
> 配套：`设计方案.md`、`工程选型与一期实施方案.md`（v1.1 §7 偏差表）、`步骤.md`、`README.md`、`简历写法.md`

---

## 已经完成

### 产品闭环

- [x] 上传照片 → 自然语言指令 → 出调色结果（费用 0 元，不调用生图 API）
- [x] **8 种**摄影 Look：`film_portra` `cinematic_teal_orange` `hk_night` `mono_bw` `vintage_fade` `golden_hour` `cool_steel` `matte_film`
- [x] `.cube` 3D LUT 引擎（自烘焙，格式对齐 CubeLUT / Premiere）
- [x] 多轮 `thread_id` 参数补丁；Job `result` / `compare`
- [x] Gradio + FastAPI；`samples/` 示意对比条
- [x] 15 个 Style Pack YAML + Chroma RAG（含分镜/材质占位文）
- [x] `image.2d` 硅基流动 + 万相备路 + 日/项目预算熔断；无 Key 失败关闭；2D 可重试 1 次
- [x] Docker Compose（`api` + `ui`）与 `STYLEFORGE_DATA_DIR` 卷
- [x] Job SQLite WAL；`queued → running → succeeded/failed`；非法跳转拒绝
- [x] `scripts/eval_report.py` + `pytest -m golden`（E1/E2/E4 mock；E3 live skip）

### Agent / 工程（简历可讲）

- [x] LangGraph：`route → perceive → execute → critique`（Look）；问答 / 3D 在 route 后结束
- [x] `perceive`：亮度/色温/人脸启发式，无风格口令时建议 Look
- [x] `critique`：SSIM + 尺寸，最多再 execute 1 次；MCTS 开关默认关
- [x] 关键词路由 + 可选 DeepSeek 结构化路由（单测可 mock）
- [x] Renderer 协议；厂商 URL 只在 `app/providers/`
- [x] SQLite Job / 资产 / 花费账本；3D/视频 501
- [x] OpenAPI 含 jobs / chat / styles / assets / capabilities

---

## 下一步需要完成（非代码能代替）

### P0 作品集素材

- [ ] 用自有版权真片替换 `samples/` 合成图，并作为黄金集 P 子集

### P2 加深（简历已可写「架构已落地、MCTS 关闭」）

- [ ] MCTS 子图真正搜索（默认仍关）
- [ ] VLM 评测抽样；可选 CLIP

### P3 双轨（接口已 501）

- [ ] 7 种艺术风格作品集真出图（需 Key 与额度，产物放 `tests/eval/m2/`）
- [ ] `asset.3d` / `video.*` 真 Provider

---

## 当前验收口令

1. `pytest -q` 全绿（含 golden；`tests/live` skip）
2. `python scripts/eval_report.py` 写出 `data/eval/report.md`
3. 胶片 / 黑白 / 黄金时刻等口令出 LUT；「再暗一点」保持 style
4. 「水彩和水墨差在哪」无 job；无 Key 时「改成水墨画」`PROVIDER_NOT_CONFIGURED`
5. `docker compose config` 能解析

简历文案用 [`简历写法.md`](./简历写法.md)。不要写「已复现 PhotoAgent」或「10 种风格都能生图」。
