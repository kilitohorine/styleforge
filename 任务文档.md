# 任务文档

> 对应仓库当前版本：P1 image.2d + 预算熔断（硅基流动 / Look 仍 0 元）  
> 配套：`设计方案.md`、`工程选型与一期实施方案.md`、`步骤.md`、`README.md`

---

## 已经完成

### 产品闭环

- [x] 上传照片 → 自然语言指令 → 出调色结果（费用 0 元，不调用生图 API）
- [x] 三种摄影 Look：`film_portra`（胶片暖调）、`cinematic_teal_orange`（电影青橙）、`hk_night`（港风夜景）
- [x] `.cube` 3D LUT 引擎（Adobe/IRIDAS 格式，对齐 [CubeLUT](https://cubelut.cn/index.php) / Premiere Lumetri；LUT 为仓库自烘焙，不搬运该站素材）
- [x] 多轮 `thread_id`：「再暗一点 / 再暖一点 / 少颗粒」只改参数，不丢 `style_id`
- [x] Job `trace` 含 `comparison_asset_id` + 输出 `result` / `compare` 并排图
- [x] `samples/` 示意对比条（合成场景；真片请替换为自有版权照片）
- [x] Gradio 界面（默认 http://127.0.0.1:7860）
- [x] FastAPI + OpenAPI（默认 http://127.0.0.1:8000/docs）
- [x] 10 个 Style Pack YAML（艺术 7 + 摄影 3）；`GET /v1/styles` / `POST /v1/rag/query` / `POST /v1/styles/ingest`
- [x] 无图问答走 RAG citation（水彩 vs 水墨），不创建 Job
- [x] `image.2d` 硅基流动适配器（文生图/图生图）；无 Key 失败关闭；`IMAGE_2D_BACKEND=mock` 供测试
- [x] 日预算 / 项目预算熔断：超预算禁止生图且不发 HTTP，Look 仍可用

### Agent / 工程（简历可讲）

- [x] LangGraph 图：`START → route → execute → END`（问答或无图则 route 后结束）
- [x] 关键词路由；若配置 `DEEPSEEK_API_KEY` 则优先 LLM 结构化路由
- [x] Renderer 协议雏形：Look 走 OpenCV；`image.2d` 走 `Image2DRenderer`，厂商 URL 只在 `app/providers/`
- [x] 统一 Job 信封字段：`modality` / `style_id` / `trace` / `actual_cost_cny`
- [x] 资产上传下载、SQLite 任务记录（数据目录由 `STYLEFORGE_DATA_DIR` 控制，不写死盘符）
- [x] `asset.3d` / `video.*` 返回 HTTP 501（`MODALITY_RESERVED`）
- [x] `.env` 不进 git；`requirements.txt` 钉住可运行的 LangGraph 0.3 组合
- [x] 自动化测试：`pytest -q`（Look 像素变化、关键词路由、health、3D 501、上传+chat 出图、RAG 夜景霓虹命中）

### 文档

- [x] 设计方案、工程选型、逐步操作手册、README（含 PhotoAgent 引用与简历写法）

---

## 下一步需要完成

按优先级，**先做完再往下**，避免跳到训练或长视频。

### P0 — 把闭环做成可展示作品集（仍 0 生图费）

- [x] `.cube` LUT + 多轮补丁 + 并排对比（本步已完成）
- [ ] 用 5～10 张**自有版权真实照片**替换 `samples/` 合成图（胶片 / 青橙 / 港风）
- [ ] 可选：填入 DeepSeek Key，把路由从关键词换成 LLM，并补 3 条对话回归测试

### P1 — 一期核心剩余（对应原 M1/M2）

- [x] Style Pack YAML + RAG（Chroma + n-gram 哈希嵌入；BGE-small-zh 可后续切换），10 个风格可检索
- [x] 艺术风格问答走 RAG citation，不再只用写死文案
- [x] 接入一个低价 2D Provider（硅基流动 Kolors）：`image.2d` 文生图 / 图生图（无 Key 不扣费）
- [x] 日预算 / 项目预算熔断：超预算禁止生图，Look 仍可用
- [ ] Docker Compose 与本机 venv 行为一致（迁服务器用）

### P2 — 更接近 PhotoAgent 论文（默认关，简历写「架构预留」）

- [ ] `perceive` 节点：场景分类（人像/风光/夜景等）+ 提出 K 个原子动作
- [ ] `critique` 节点：SSIM / 人脸存在 / Look 方向启发式，失败则回退上一张
- [ ] 有限闭环：`max_step=2～3` 的 perceive→plan→execute→critique
- [ ] MCTS 做成可开关子图，默认贪心，避免打穿 API 预算

### P3 — 双轨扩展（接口已 501，暂不实现）

- [ ] `image.2d` 七种艺术风格（水彩、田园动画风、赛博、水墨等）作品集出图
- [ ] `asset.3d` Provider stub → 真实成模 API
- [ ] `video.clip` / `video.long`：短镜头 + 视频 LUT（复用 Look），禁止整段生成长视频
- [ ] LoRA / 云上训练：仅当某风格稳定失败且另批预算

---

## 当前验收口令（P1 image.2d）

1. `python -m pytest -q` 全绿（含日预算=0 时 2D 为 `BUDGET_EXCEEDED` 且 `generate_calls` 为空；Look 仍成功）  
2. 「水彩和水墨差在哪」无 job；「改成水墨画」+ `IMAGE_2D_BACKEND=mock` 有 `image.2d` job  
3. 无 `SILICONFLOW_API_KEY` 时真实 backend 失败码 `PROVIDER_NOT_CONFIGURED`，摄影 LUT 仍 0 元  
4. `GET /v1/budget` 可读；`asset.3d` → 501  

README / 简历不要写「已复现 PhotoAgent」或「已支持 10 种风格出图」。2D 请写「硅基流动适配器 + 预算熔断；未充值则失败关闭」。
