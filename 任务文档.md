# 任务文档

> 对应仓库当前版本：一期收口（8 种 Look LUT + perceive/critique + Docker + RAG/2D 预算）  
> 配套：`设计方案.md`、`工程选型与一期实施方案.md`、`步骤.md`、`README.md`

---

## 已经完成

### 产品闭环

- [x] 上传照片 → 自然语言指令 → 出调色结果（费用 0 元，不调用生图 API）
- [x] **8 种**摄影 Look：`film_portra` `cinematic_teal_orange` `hk_night` `mono_bw` `vintage_fade` `golden_hour` `cool_steel` `matte_film`
- [x] `.cube` 3D LUT 引擎（自烘焙，格式对齐 CubeLUT / Premiere）
- [x] 多轮 `thread_id` 参数补丁；Job `result` / `compare`
- [x] Gradio + FastAPI；`samples/` 示意对比条
- [x] 15 个 Style Pack YAML + Chroma RAG
- [x] `image.2d` 硅基流动 + 日/项目预算熔断；无 Key 失败关闭；2D 可重试 1 次
- [x] Docker Compose（`api` + `ui`）与 `STYLEFORGE_DATA_DIR` 卷

### Agent / 工程（简历可讲）

- [x] LangGraph：`route → perceive → execute → critique`（Look）；问答 / 3D 在 route 后结束
- [x] `perceive`：亮度/色温/人脸启发式，无风格口令时建议 Look
- [x] `critique`：SSIM + 尺寸，最多再 execute 1 次；MCTS 开关默认关
- [x] 关键词路由 + 可选 DeepSeek 结构化路由（3 条对话单测可 mock）
- [x] Renderer 协议；厂商 URL 只在 `app/providers/`
- [x] SQLite Job / 资产 / 花费账本；3D/视频 501

---

## 下一步需要完成

### P0 作品集素材

- [ ] 用自有版权真片替换 `samples/` 合成图

### P2 加深（简历已可写「架构已落地、MCTS 关闭」）

- [ ] MCTS 子图真正搜索（默认仍关）
- [ ] VLM 评测抽样

### P3 双轨（接口已 501）

- [ ] 7 种艺术风格作品集真出图（需 Key 与额度）
- [ ] `asset.3d` / `video.*` 真 Provider

---

## 当前验收口令（一期收口）

1. `pytest -q` 全绿（8 个 cube、夜景「修好」建议 `hk_night`、Docker 文件存在）  
2. 胶片 / 黑白 / 黄金时刻等口令出 LUT；「再暗一点」保持 style  
3. 「水彩和水墨差在哪」无 job；无 Key 时「改成水墨画」`PROVIDER_NOT_CONFIGURED`  
4. `docker compose config` 能解析（本机有 Docker 时再 `up --build`）

简历不要写「已复现 PhotoAgent」或「10 种风格都能生图」。
