# StyleForge 简历写法

仓库：https://github.com/kilitohorine/styleforge  
配套：`工程选型与一期实施方案.md` §7（落地偏差）、`README.md`、`任务文档.md`

本文只写 **当前仓库真实具备的能力**。面试官如果 clone 下来跑，应和这里一致。

---

## 一句话（中文，约 80 字）

独立完成修图 Agent StyleForge：LangGraph 编排 route→perceive→execute→critique；8 种摄影 Look 为自烘焙 Adobe `.cube` 3D LUT（OpenCV，零推理成本）；YAML Style Pack + Chroma RAG；2D 生图经硅基流动/万相适配器，带日与项目预算熔断。参考 PhotoAgent 闭环，非论文官方复现。

## One-liner (English)

Built StyleForge, a LangGraph photo-styling agent (route→perceive→execute→critique) with 8 self-baked `.cube` LUTs, Chroma style-pack RAG, and budget-capped SiliconFlow / DashScope image.2d adapters. Inspired by PhotoAgent; not an official reproduction.

---

## 项目描述（可直接贴「项目经历」）

**StyleForge | 个人项目 | Python / LangGraph / FastAPI / Gradio**

- 用 LangGraph 实现修图流水线：路由（关键词 + 可选 DeepSeek 结构化输出）→ 场景感知（亮度/色温/Haar 人脸）→ 执行 LUT 或 2D Provider → SSIM 评价并最多重试一次。
- 摄影路径 8 个 Look（胶片暖调、电影青橙、港风夜景、黑白、复古、黄金时刻、冷调、哑光），自烘焙 17³ `.cube`，格式对齐 Premiere / CubeLUT；多轮「再暗一点」只改参数、不丢 `style_id`。
- 15 个 Style Pack 入 Chroma；问答返回 citation，不创建 Job。3D / 长视频接口 501 占位。
- FastAPI + SQLite WAL Job 状态机 + 花费账本；生图前校验日预算/项目预算（默认 20 / 500 元）；无 Key 失败关闭。Docker Compose 提供 api + ui。
- mock 黄金集：`python scripts/eval_report.py` 与 `pytest -m golden`（30 条路由、Look SSIM、RAG Recall@3）。

**技术栈**：Python 3.12，LangGraph 0.3，LangChain-OpenAI 兼容 DeepSeek，FastAPI，Pydantic v2，Chroma，OpenCV，Gradio，pytest。

---

## 面试时建议展开的 4 个点

1. **为什么 Look 不用生成模型**：像素网格不变，SSIM 高，费用 0，更接近 Lumetri，而不是滤镜重绘。
2. **图为什么这样切**：LangGraph 需要显式边才能测「问答不渲染 / 调色走 LUT / 改成水墨才 2D」；节点不能与 state key 同名。
3. **预算熔断写在发 HTTP 之前**：`BUDGET_EXCEEDED` 时 Provider 调用计数为 0，单测可证。
4. **版权**：田园动画风叫 `pastoral_anime`，prompt 不含吉卜力商标；产品不宣称官方授权。

现场可画：

```
START → route → perceive → execute → critique → (失败则再 execute 一次) END
              ↘ 问答 / 无图 / 3D END
              ↘ image.2d execute END
```

---

## 禁止写进简历的句子

| 不要写 | 原因 |
| --- | --- |
| 开发了 / 复现了 PhotoAgent | 论文是 Yao et al.；本仓库只是受其闭环启发 |
| 已支持 10 种风格出图 | 8 个 Look 可本地出图；7 个艺术风格无 Key 时不出图 |
| 已复现 MCTS / GRPO / UGC Reward | 开关默认关，未实现搜索与训练 |
| 用 BGE / CLIP / MediaPipe 做了一期评测 | 默认 n-gram embedding、自实现 SSIM、Haar |
| 本地部署了 FLUX | 一期明确不下载 17GB+ 权重 |
| 3D / 视频已上线 | 仅 `reserved` + HTTP 501 |

---

## 数字怎么写才站得住

- **8** 个摄影 Look，**15** 个可检索 Pack：可以写（`GET /v1/styles`）。
- **0 元/张** 调色：可以写（Look Job `actual_cost_cny==0`）。
- **500 元项目顶 / 20 元日顶**：可以写「熔断逻辑已实现」；不要写「已经花完 500 元做了评测集」，除非你真有账单。
- **pytest 全绿**：可以写工程测试；不要把 mock 黄金集说成「12 张版权成片盲选 70%」。

---

## 若面试官问「还缺什么」

如实说即可，显得靠谱：

- 作品集需要换成自有版权真片（现在 `samples/` 是示意）。
- 7 种艺术风格要配硅基流动或万相 Key 才能出作品集图。
- 论文里的 MCTS 审美搜索与 GRPO 不在一期范围。

---

## 演示 8 分钟（建议顺序）

1. `pytest -q` 或打开 `GET /v1/capabilities`
2. 上传照片 → 「做成胶片暖调」→ 看对比条与 `trace.lut`
3. 「再暗一点」→ `style_id` 不变
4. 「水彩和水墨差在哪」→ 无 `job_id`，有 citation
5. 「改成水墨画」→ 无 Key 时 `PROVIDER_NOT_CONFIGURED`（证明熔断，不是崩了）
6. 仓库 README 底部 PhotoAgent citation，口头声明非官方实现
