# 风格化渲染 Agent 完整设计方案

> 项目代号：`StyleForge Agent`  
> 文档版本：v1.0  
> 日期：2026-08-28  
> 约束：一期成本 ≤ **500 元**；一期只做 **2D 图像 + 摄影 Look**；架构必须可接 **3D** 与 **长视频** 两条后续路线。

---

## 0. 先回答你最关心的两件事

### 0.1 现在要不要在服务器上训练？

**不建议。一期明确禁止在云服务器上做模型训练 / 全量微调。**

原因很直接：

| 路径 | 一期能否塞进 500 元 | 对「会做 Agent」招聘是否加分 | 风险 |
| --- | --- | --- | --- |
| **不训练**：LangGraph Agent + RAG 风格配方 + 现成图生图/文生图 API + 本地摄影调色 | **能** | **加分最大**：工具、RAG、图编排、评测、接口全覆盖 | 低 |
| **轻量 LoRA**（8–10 个风格各训一份） | 算力大约 80–200 元，但**数据集、版权、调参、失败重训**会把总账打穿 | 加分有限，偏 CV 训练而非 Agent | 中高 |
| **全量训练 / 继续预训练** | **不可能**，单卡包月都远超 500 | 与本项目目标错位 | 极高 |

一期要证明的不是「你会训扩散模型」，而是：

- 会把自然语言意图拆成可执行的渲染流水线  
- 会用 RAG 管风格知识，而不是把 prompt 写死在代码里  
- 会设计可插拔的渲染后端（今天是 2D，明天可以是 3D / 视频）  
- 会做异步任务、结构化接口、评测与失败重试  

这些才是招聘里 Agent 岗真正看的环节。训练作为**二期可选插件**预留，不作为一期交付物。

### 0.2 一期怎么覆盖 8–10 种风格，还能「替代 Pr 做相片渲染」？

风格 **不要靠训练 8–10 个模型**，而要靠三层叠加：

1. **Style Pack（风格配方）**：每种风格一份 YAML + 少量参考图描述，进向量库，供 RAG 检索。  
2. **渲染后端**：图生图 / 文生图 API（Flux Kontext、万相、硅基流动等），用配方里的正负向 prompt、强度、分辨率。  
3. **摄影 Look 引擎（替代 Pr 的 Lumetri 部分）**：对真实照片做曝光、对比、HSL、胶片颗粒、LUT 风格曲线——**零生成成本**，这是「相片渲染」的主路径。

「替代 Pr」在一期的诚实定义是：

> 替代 Premiere / Lightroom 里 **Look / Lumetri / LUT / 胶片调色** 这一段，而不是替代剪辑时间线、字幕、音频、交付编码全套 NLE。

长视频方向二期再接：分镜 Agent → 镜头级生成/调色 → 拼接。接口从第一天就按这个形状设计。

---

## 1. 产品定位

做一个 **LangChain / LangGraph 驱动的风格化渲染 Agent**，用户用自然语言或结构化请求，完成：

- 插画 / 概念图的 **2D 风格化**（水彩、吉卜力风、赛博朋克、水墨等）  
- 摄影相片的 **Look 渲染**（电影感、胶片、港风、赛博夜景等，对标 Pr 调色）  
- 后续可在**同一套 Job 接口**上接入 **3D 资产风格化** 和 **长视频风格化/辅助成片**

招聘叙事一句话：

> 这是一个多模态渲染编排 Agent：LangGraph 管规划与工具调用，RAG 管风格知识，渲染后端可插拔，评测闭环可重试。

---

## 2. 范围与硬约束

### 2.1 一期做 / 不做

| 做 | 不做 |
| --- | --- |
| 对话式 Agent（规划、工具、记忆、重试） | 云上训练 / 全量微调 |
| RAG 风格知识库 + Style Pack | 吉卜力等 IP 的官方素材训练 |
| 2D 文生图 / 图生图风格化 | 一键生成 10 分钟成片 |
| 摄影 Look（参数调色 + 可选 AI 风格化） | 完整 Pr 时间线 / 音频 / 字幕工程 |
| 统一异步 Job 接口 | 包月 GPU 常驻 |
| 8–10 种内置风格配方 | 自研 3D 引擎 |
| 本地 FastAPI + 本机存储 | 一上来就做 SaaS 计费系统 |

### 2.2 硬约束（违反即视为方案失败）

1. **预算硬顶**：一期现金支出 ≤ 500 元（含 LLM、生图 API、可选按量 GPU 试跑）。  
2. **禁止一期训练**：不允许租卡做 LoRA / DreamBooth / 全量训练，除非单独立项且另批预算。  
3. **统一信封**：所有产出必须走 `RenderJob`，字段含 `modality`，禁止业务代码直接调某个厂商 SDK。  
4. **风格外置**：风格只存在于 Style Pack + RAG，禁止 `if style == "ghibli"` 写死 prompt。  
5. **可追溯**：每次产出必须落库 `model / prompt / seed / style_id / params / cost_estimate`。  
6. **可切换后端**：至少预留 2 个 2D Provider（例如硅基流动 + 阿里万相），本地 ComfyUI 作为第三 Provider 接口。  
7. **异步**：生图 / 生视频 / 3D 一律异步 Job，同步 HTTP 只接受「提交」和「查询」。  
8. **密钥不出库**：`.env` 管理，禁止提交；日志脱敏。  
9. **版权墙**：内置风格用「风格描述」而非受版权保护的原作图训练；吉卜力命名为「田园动画风 / pastoral_anime」，产品文案不得宣称官方授权。  
10. **双轨预留**：`modality` 从第一天包含 `image.2d`、`image.photo_look`、`asset.3d`、`video.clip`、`video.long`，后三者一期返回 `501 Not Implemented` 但 schema 稳定。

---

## 3. 功能清单

### 3.1 核心功能（一期必须交付）

对应招聘里「能把 Agent 项目讲清楚」的最小闭环。

| ID | 功能 | 用户感知 | 技术要点 |
| --- | --- | --- | --- |
| C1 | 自然语言意图解析 | 「把这张夜景做成赛博朋克，保留人脸」 | LangGraph 规划节点 + 结构化输出 |
| C2 | 风格选择与配方检索 | 8–10 种风格，可追问「更湿润一点」 | RAG：Style Pack + 摄影 Look 卡片 |
| C3 | 2D 风格化渲染 | 文生图 / 图生图 | Provider 适配器，img2img strength 可控 |
| C4 | 摄影 Look 渲染（替代 Pr 调色） | 上传 RAW/JPG，套电影感 / 胶片 / 港风 | 参数调色管线（主）+ 可选 AI 二次风格化 |
| C5 | 多轮改稿 | 「暗一点、少颗粒、别动构图」 | Checkpoint 记忆 + 增量参数补丁 |
| C6 | 异步任务与资产 | 提交后轮询，下载前后对比图 | Job 状态机 + 本地对象存储 |
| C7 | 质量自检与有限重试 | 明显崩脸 / 风格跑偏时自动再跑 1 次 | 规则 + 可选 VLM 打分 |
| C8 | 风格知识问答 | 「水彩和水墨差在哪、摄影怎么选 LUT」 | 同一套 RAG，不走渲染工具 |
| C9 | 成本记账 | 每次 Job 估算与累计消耗 | 按 Provider 单价表 |

### 3.2 一期风格目录（10 种，可直接做配方）

艺术向（插画 / 概念图）与摄影向（相片 Look）分开，避免「水彩滤镜套在人像上不可控」。

**A. 艺术风格（图生图 / 文生图）**

| style_id | 名称 | 典型用途 |
| --- | --- | --- |
| `watercolor` | 水彩 | 插画、风景、绘本 |
| `pastoral_anime` | 田园动画风（吉卜力启发） | 场景、角色设定，**非官方 IP** |
| `cyberpunk` | 赛博朋克 | 夜景、科幻、霓虹 |
| `ink_wash` | 水墨 | 山水、留白、书法感 |
| `oil_paint` | 油画 | 肖像、静物 |
| `pixel` | 像素 | 游戏概念、复古 |
| `flat_illustration` | 扁平插画 | 海报、图标风场景 |

**B. 摄影 Look（替代 Pr / LR 调色，优先走参数引擎）**

| style_id | 名称 | 对标 |
| --- | --- | --- |
| `cinematic_teal_orange` | 电影青橙 | Lumetri 电影感 |
| `film_portra` | 胶片暖调 | 人像、婚礼、日系 |
| `hk_night` | 港风夜景 | 街拍、霓虹、潮湿路面 |

一期默认 **10 种**。用户说「8–10 种」时，可先上前 8 个，后 2 个当配方扩展，不改接口。

### 3.3 后续可扩展功能（明确分两条发展轨）

两条轨 **共享 Agent、Job、RAG、评测、资产库**，只换 Renderer 插件。

#### 轨 1：3D 图像 / 资产风格化

| 阶段 | 能力 | 建议实现 |
| --- | --- | --- |
| 3D-α | 单图 → 粗 mesh / 高斯 / 多视图 | 调用 Hunyuan3D / Tripo / Meshy API |
| 3D-β | 风格化贴图 / 材质 | 2D 风格化 UV 或生成 PBR 贴图后再烘焙 |
| 3D-γ | 风格化场景预览（静帧） | Blender 无头渲染 或 引擎截图 |
| 3D-δ | 可选：自训风格 LoRA 作用到贴图生成 | **这时才考虑训练** |

一期接口：`POST /v1/jobs` 且 `modality=asset.3d` → `501`，但请求体已定义。

#### 轨 2：长视频风格化 / 辅助成片（「更像替代 Pr」的完整形态）

长视频 **禁止** 设计成「一个 API 吐 10 分钟」。必须拆成 Agent 流水线：

```
剧本/素材 → 分镜表 → 镜头级（生成或调色）→ 一致性检查 → 拼接 → 成片 Look
```

| 阶段 | 能力 | 说明 |
| --- | --- | --- |
| V-α | 短镜头 4–8 秒 图生视频 | 万相 / 可灵 / Seedance，成本按秒 |
| V-β | 镜头级摄影 Look（视频版 Lumetri） | 把一期调色引擎从帧扩展到视频（ffmpeg + LUT） |
| V-γ | 分镜 Agent + 镜头列表 RAG | 真正体现 Agent 价值 |
| V-δ | 角色/场景一致性（IP-Adapter / 参考帧） | 长视频成败关键 |
| V-ε | 粗剪时间线导出（EDL / 简单拼接） | 「辅助 Pr」而不是一次性替换专业剪辑 |

一期接口：`video.clip` / `video.long` schema 冻结，实现返回 `501`。

#### 其它横向扩展（两条轨都用得上）

- 新 Style Pack 热加载（不发版加风格）  
- 用户参考图 / 品牌色约束  
- 批量写真（20 张同一 Look）  
- Human-in-the-loop 打分，沉淀成后期 LoRA 数据  
- LangSmith 追踪、评测集、回归  
- 可选：AutoDL 上 **推理** ComfyUI，不是训练  

---

## 4. 总体架构（为双轨预留）

### 4.1 分层

```
┌─────────────────────────────────────────────────────────┐
│  接入层  FastAPI / OpenAPI / 可选简单 Web               │
│  Chat  · Jobs  · Styles  · Assets  · RAG Admin          │
└───────────────────────────┬─────────────────────────────┘
                            │ 统一 DTO（见第 6 节）
┌───────────────────────────▼─────────────────────────────┐
│  编排层  LangGraph  （招聘核心）                         │
│  ingest → route → retrieve → plan → execute → critique  │
│  Memory: 线程级 checkpoint + 用户偏好                    │
└───────────────┬───────────────────────────┬─────────────┘
                │                           │
     ┌──────────▼──────────┐     ┌──────────▼──────────┐
     │ 知识层 RAG          │     │ 工具层 Tools        │
     │ Style Pack          │     │ renderer.2d         │
     │ Photo Look 卡片     │     │ renderer.photo_look │
     │ 失败手册 / 镜头语法 │     │ renderer.3d  (stub) │
     │ Chroma + 本地 embedding│   │ renderer.video(stub)│
     └─────────────────────┘     │ critic / cost       │
                                 └──────────┬──────────┘
                                            │
┌───────────────────────────────────────────▼─────────────┐
│  能力层  Provider 插件（只换这里，上面不动）              │
│  2D: SiliconFlow / DashScope / ComfyUI                  │
│  Photo: OpenCV-LUT / Pillow / ffmpeg                    │
│  3D: Hunyuan3D / Tripo / Meshy                          │
│  Video: Wanxiang / Kling / 本地 Wan                     │
└─────────────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────────────┐
│  状态层  SQLite（Job） + 本地磁盘（资产） + 可选 Redis    │
└─────────────────────────────────────────────────────────┘
```

### 4.2 为什么这样拆

- **招聘**：能单独讲清 Graph、Tool、RAG、Memory、Evaluator，而不是一个调 API 的脚本。  
- **3D / 视频**：只加 Renderer + 对应 Style Pack 子集，Job 状态机不用重写。  
- **成本**：贵的能力（生图、生视频、3D）全部隔离在 Provider，可按 Job 熔断。  
- **训练**：未来 LoRA 只是某个 Provider 的 `model_ref`，不污染 Agent。

### 4.3 LangGraph 主图（一期实现）

```
                 ┌────────────┐
                 │  UserTurn  │
                 └─────┬──────┘
                       ▼
                 ┌────────────┐
                 │  Router    │  问答 / 渲染 / 改稿 / 批处理
                 └─────┬──────┘
           ┌───────────┼───────────┐
           ▼           ▼           ▼
        RAG-QA     Retrieve     PatchParams
                       │
                       ▼
                    Planner
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
     PhotoLook      Image2D      Stub3D/Video
          │            │            │
          └────────────┼────────────┘
                       ▼
                   Critique
                    /    \
                 retry   Finish
```

Router 用小模型或规则+LLM 结构化输出，避免全程大模型。

---

## 5. 技术骨架

### 5.1 推荐技术栈（一期）

| 层 | 选型 | 理由 |
| --- | --- | --- |
| Agent | **LangGraph** + LangChain 工具 | 招聘主流；有状态图、checkpoint、human-in-loop |
| LLM | **DeepSeek / Qwen**（国内 API） | 编排足够强，远比 GPT-4o 便宜 |
| 可选视觉理解 | 通义 / GLM-4V / 硅基流动 VLM | 只在 critique 或「理解这张照片」时用 |
| 2D 生成 | **硅基流动** 为主，**阿里万相** 为备 | 按张计费，无月租 |
| 摄影 Look | **OpenCV + Pillow + 3D LUT / 曲线 JSON** | 零推理成本，最像 Pr |
| RAG | **Chroma** + 本地 embedding（BGE-small / bge-m3 CPU） | 0 元 |
| API | **FastAPI** + Pydantic v2 | 接口即契约 |
| 任务 | SQLite + 后台 worker（先线程池，后可换 ARQ） | 够用 |
| 观测 | 结构化日志；可选 LangSmith 免费档 | 方便写进简历 |
| 前端 | 极简（Gradio 或一个 HTML） | 一期不为 UI 烧时间 |

不选 LangGraph Cloud、不选向量云库、不选包月 GPU。

### 5.2 仓库结构（按此搭，后续 3D/视频只加插件目录）

```
styleforge/
  app/
    api/                  # FastAPI 路由，薄，只做校验与鉴权
    schemas/              # 对外 DTO = 对内 Job 信封
    graphs/               # LangGraph 定义
    nodes/                # router / retrieve / plan / critique
    tools/                # 给 Agent 的工具封装
    rag/
      ingest.py
      retriever.py
      packs/              # Style Pack YAML + markdown
    renderers/
      base.py             # Renderer Protocol
      image_2d/
      photo_look/
      asset_3d/           # stub
      video_clip/         # stub
      video_long/         # stub
    providers/            # 厂商 SDK 适配，禁止泄漏到 nodes
    jobs/                 # 状态机、worker
    eval/                 # 规则评测、可选 VLM
    cost/                 # 单价表
  data/
    chroma/
    assets/
    jobs.db
  tests/
  .env.example
  设计方案.md
```

### 5.3 核心抽象（硬约束落地）

```python
# renderers/base.py（示意，一期按此实现）
from typing import Protocol
from app.schemas.job import RenderJob, RenderResult

class Renderer(Protocol):
    modality: str  # image.2d | image.photo_look | asset.3d | video.clip | video.long

    def capabilities(self) -> dict: ...
    def estimate_cost(self, job: RenderJob) -> float: ...
    def submit(self, job: RenderJob) -> str: ...   # 返回 provider_task_id
    def poll(self, provider_task_id: str) -> RenderResult: ...
```

Planner **只认 Renderer 接口**，不认 Flux / 万相 / Meshy。

新增 3D：实现一个 `Hunyuan3DRenderer`，在注册表挂上 `asset.3d`。  
新增长视频：先实现 `ShotPlanner` 节点，再对每个 shot 调 `video.clip`。

### 5.4 Style Pack 格式（风格外置）

```yaml
# rag/packs/cyberpunk.yaml
style_id: cyberpunk
name: 赛博朋克
domain: [image.2d, image.photo_look, video.clip]  # 声明哪些模态能用
version: 1
positive: |
  cyberpunk, neon rain, reflective wet asphalt, magenta-cyan lighting,
  dense signage, cinematic wide, high contrast
negative: |
  daylight, pastoral, watercolor paper, low contrast, blurry face
photo_look:
  lut: luts/cyber_night.cube   # 摄影路径优先用 LUT，没有再走 AI
  exposure: -0.3
  contrast: 0.25
  grain: 0.12
i2i:
  strength: 0.55
  keep: [face, layout]
legal: original_descriptors_only
```

RAG 入库的是这段 YAML 展开后的说明文 + 适用场景，不是只存 style_id。

---

## 6. 接口规范

对外 **REST + JSON**。异步渲染。版本前缀 `/v1`。  
所有错误体统一：`{ "error": { "code": "...", "message": "...", "retryable": false } }`。

### 6.1 公共类型

```json
{
  "modality": "image.2d | image.photo_look | asset.3d | video.clip | video.long",
  "style_id": "cyberpunk",
  "input_assets": [{ "asset_id": "a_123", "role": "source|reference|mask" }],
  "prompt": "保留人物，把背景改成雨夜霓虹",
  "constraints": {
    "keep_face": true,
    "keep_layout": true,
    "aspect": "3:2",
    "resolution": "1024x1024",
    "seed": 42
  },
  "overrides": { "strength": 0.4, "grain": 0.05 },
  "budget_cny_max": 1.0
}
```

`video.long` 额外字段（一期就定义，避免二期改合约）：

```json
{
  "target_duration_sec": 180,
  "source": { "type": "images|clips|script", "asset_ids": [] },
  "pipeline": ["shot_plan", "per_shot_render_or_grade", "stitch", "master_look"]
}
```

`asset.3d` 额外字段：

```json
{
  "output": "mesh_glb|multiview|gaussian",
  "texture_style_id": "ink_wash"
}
```

### 6.2 HTTP 接口

#### 健康与能力

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/health` | 存活 |
| GET | `/v1/capabilities` | 当前已实现的 modality、provider、风格数 |

一期 `capabilities` 示例：

```json
{
  "modalities": {
    "image.2d": { "status": "ready", "providers": ["siliconflow", "wanxiang"] },
    "image.photo_look": { "status": "ready", "providers": ["opencv_lut"] },
    "asset.3d": { "status": "reserved" },
    "video.clip": { "status": "reserved" },
    "video.long": { "status": "reserved" }
  }
}
```

#### 风格

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/v1/styles` | 列表，可按 `domain` 过滤 |
| GET | `/v1/styles/{style_id}` | 配方摘要（不含完整商业 prompt 也可） |
| POST | `/v1/styles/ingest` | 管理端：热加载新 YAML 进 RAG |

#### 对话 Agent（招聘演示主入口）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/chat` | 同步返回「文本 + 可能触发的 job_id」 |
| GET | `/v1/chat/{thread_id}` | 拉取该线程消息与关联 Job |

请求：

```json
{
  "thread_id": "optional-uuid",
  "message": "把这张照片做成胶片暖调，脸别磨皮",
  "asset_ids": ["a_123"]
}
```

响应：

```json
{
  "thread_id": "t_1",
  "reply": "将使用 film_portra Look，保留皮肤质感，已提交任务。",
  "job_id": "j_1",
  "citations": ["style:film_portra", "pack:photo_looks"]
}
```

#### 资产

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/assets` | `multipart/form-data` 上传图/短视频 |
| GET | `/v1/assets/{asset_id}` | 元数据 |
| GET | `/v1/assets/{asset_id}/file` | 下载 |

#### 任务（核心渲染接口，3D/视频复用）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/jobs` | 创建渲染任务 |
| GET | `/v1/jobs/{job_id}` | 状态：queued / running / succeeded / failed / cancelled |
| POST | `/v1/jobs/{job_id}/cancel` | 取消 |
| POST | `/v1/jobs/{job_id}/retry` | 用同一信封重试 |
| GET | `/v1/jobs/{job_id}/events` | 可选 SSE，便于前端 |

`POST /v1/jobs` 请求即第 6.1 节信封。  
响应：

```json
{
  "job_id": "j_1",
  "status": "queued",
  "modality": "image.photo_look",
  "estimated_cost_cny": 0.0
}
```

`GET /v1/jobs/{id}` 成功时：

```json
{
  "job_id": "j_1",
  "status": "succeeded",
  "actual_cost_cny": 0.0,
  "outputs": [{ "asset_id": "a_out", "role": "result" }],
  "trace": {
    "style_id": "film_portra",
    "renderer": "photo_look.opencv_lut",
    "seed": null,
    "prompt": null,
    "params": { "exposure": 0.1, "grain": 0.08 }
  }
}
```

未实现模态：

```json
{
  "error": {
    "code": "MODALITY_RESERVED",
    "message": "asset.3d is reserved for phase-2; schema accepted.",
    "retryable": false
  }
}
```
HTTP **501**。

#### RAG 管理（演示「会做知识入库」）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/v1/rag/documents` | 上传 markdown / yaml |
| POST | `/v1/rag/query` | 调试检索，不走 Agent |

### 6.3 Agent 工具接口（对内，也要规定）

给 LangGraph 的 tools 必须与 HTTP 同构，避免两套语义：

| Tool | 对应能力 |
| --- | --- |
| `list_styles` | 按 domain 列风格 |
| `retrieve_style_knowledge` | RAG |
| `upload_noop` | 资产已在 HTTP 层上传，Agent 只引用 id |
| `submit_render_job` | 创建 Job |
| `get_job` | 轮询 |
| `estimate_cost` | 提交前熔断 |
| `critique_output` | 规则 / VLM |
| `patch_job_params` | 多轮改稿 |

禁止 Agent 直接拿用户图调 `requests.post(厂商URL)`。

### 6.4 Job 状态机

```
queued → running → succeeded
                 → failed → (retry) → running
                 → cancelled
```

硬规则：`budget_cny_max` 在 `running` 前检查；超预算直接 `failed` 且 `code=BUDGET_EXCEEDED`。

---

## 7. RAG 设计（必须有，且服务渲染而不只是聊天）

### 7.1 知识分片

| 集合 | 内容 | 何时检索 |
| --- | --- | --- |
| `styles` | Style Pack 展开文本、适用/禁用场景 | 每次渲染 |
| `photo_looks` | 胶片、青橙、港风等调色说明书、参数范围 | photo_look / 电影感 2D |
| `playbooks` | 崩脸、风格泄漏、过饱和时怎么改 strength | critique 失败后 |
| `shot_grammar` | 分镜、镜头运动、转场（**一期就入库少量**） | 为长视频轨占位 |
| `material_3d` | PBR、风格化材质关键词（占位文档） | 为 3D 轨占位 |

一期就把 `shot_grammar` 和 `material_3d` 放进库（哪怕各 3–5 篇），简历上可以说「知识层已按多模态预留」，而不是空目录。

### 7.2 检索策略

- 用户话术 → embedding 召回 Top-k  
- 用 `style_id` / `modality` 做 metadata 过滤  
- Planner 把检索到的 **参数区间** 写入 Job，而不是把长文塞进生图 prompt  
- 问答路径才把原文片段作为 citation

这能避免 RAG 变成「把百科全书糊到 Flux 上」。

---

## 8. 摄影 Look：一期如何「替代 Pr」

Premiere 里相片/成片气质主要来自 **Lumetri + LUT + 颗粒 + 暗角**。一期用经典图像管线实现同一类效果，Agent 负责选 Look 和改参数。

```
原图 → 曝光/白平衡 → 对比与曲线 → HSL → 3D LUT → 颗粒/暗角 → 输出
         ↑
    Style Pack.photo_look + 用户补丁（「再暖一点」）
```

可选第二段：用户要求「变成油画」时，**先 Look 再 2D img2img**，两个 Renderer 串联，仍是一个 Job（内部 steps 数组）。

对简历的价值：不是只会调生成模型，而是知道 **生成式 + 传统调色** 该怎么由 Agent 路由。

视频轨复用同一套 Look JSON，把「单帧」换成 ffmpeg `lut3d` 滤镜，接口不变。

---

## 9. 训练策略（现在不用，以后怎么接）

### 9.1 一期（现在）

- **不训练**。风格 = 配方 + 通用模型。  
- 收集「用户选了哪张 / 打了几分」到本地 JSONL，为二期做数据，**几乎 0 成本**。

### 9.2 何时才该训练

同时满足再开训：

1. 通用模型在某风格上稳定失败（例如水墨总变成日漫）  
2. 你已有 **自己有权使用** 的 30–80 张该风格图  
3. 另有一笔 **200 元级** 算力预算  
4. 目标是 Provider 插件 `comfyui://lora/ink_wash_v1`，而不是推翻 Agent

### 9.3 二期若训练，怎么做才便宜

| 项 | 建议 |
| --- | --- |
| 平台 | AutoDL 按量，训完立刻关机 |
| 卡 | RTX 4090，约 2–3 元/小时量级 |
| 方法 | **LoRA / IP-Adapter**，禁止全量 |
| 单风格 | 约 2–6 小时，算力约 8–20 元 |
| 8–10 风格 | 仅算力约 80–200 元；**版权与洗数据才是成本** |
| 接入 | `providers/comfyui.py` 的 `lora_name` 字段，Style Pack 增加 `model_ref` |

**吉卜力 / 明确 IP：永远不要用官方截帧训练。** 田园动画风用自己的绘制或明确可商用素材。

3D / 视频训练更贵（数据与卡时），应排在 2D LoRA 之后，且仍走同一 `model_ref` 机制。

---

## 10. 成本预算（一期 ≤ 500 元）

以下为人民币、个人学习/作品集用量，按 2026 年公开按量价量级估算，下单前以控制台为准。

### 10.1 结论

**推荐总预算 350–480 元，默认方案不租训练卡。**  
500 元够把 Agent 闭环跑熟，并生成足够作品集图片；不够做认真的模型训练，也不够玩长视频量产。

### 10.2 一期推荐分配

| 科目 | 金额（元） | 用途 |
| --- | --- | --- |
| LLM 编排（DeepSeek / 通义） | 40 | Chat + 规划，远超演示 token 量 |
| 2D 生图 API（主成本） | 220 | 约 800–4000 张，取决于模型档位 |
| 可选 VLM 打分 | 30 | critique 抽样，不要每张都走 VLM |
| 摄影 Look | 0 | 本地 OpenCV |
| RAG / 存储 / FastAPI | 0 | 本机 |
| 按量 GPU **推理**试跑（可选） | 40 | AutoDL 数小时验证 ComfyUI 接口，**不训练** |
| 余量 / 失败重试 | 50–70 | 防超支 |
| **合计** | **380–480** | 控制在 500 内 |

### 10.3 生图单价与 220 元能买到什么

| 档位 | 大约单价 | 220 元张数 | 建议用法 |
| --- | --- | --- | --- |
| 极低价 turbo / schnell 类 | 0.02–0.05 元/张 | 4000–10000 | 调 prompt、做回归 |
| 硅基流动 FLUX.1-dev 量级 | ~0.10 元/张（按美元价折算） | ~1500+ | 作品集主图候选 |
| FLUX Kontext 编辑 | ~0.30 元/张量级 | ~700 | 图生图改风格、保内容 |
| 阿里万相文生图 | 0.20 元/张 | 1100 | 国内备路、审核友好 |

**策略**：日常迭代用低价模型；最终作品集每风格留 3–5 张走中高档。摄影 Look 大量出图 **不花生图钱**。

### 10.4 明确不要买的东西（否则 500 立刻没）

- 包月 GPU / 云主机常驻  
- GPT-4o / Claude 作为默认编排  
- 商业视频 API 做「先玩 10 分钟成片」  
- 商用 3D API 批量出模  
- LangGraph Cloud 按 action 计费  

### 10.5 二期（另立预算，不计入 500）

| 方向 | 最小试水 | 认真做 |
| --- | --- | --- |
| 新风格（只加 Pack） | 0 元 | 0 元 |
| 2D LoRA × 若干 | 单风格 ~10–20 元卡时 | 8 风格算力 ~100–200 元 + 数据 |
| 3D 试水 | API 几十元出数个 mesh | 本地 Hunyuan3D 需有卡；无卡则按次 API |
| 短视频 5–8 秒 | 万相约 0.6–1 元/秒 → 一条 5 秒约 3–5 元 | 20 条试水约 60–100 元 |
| 长视频 3 分钟 | **不要按秒生成 180 秒** | 分镜 20 镜 × 5 秒 ≈ 100 秒 → 约 60–100 元 + 调色 0 元（ffmpeg） |

长视频轨的成本控制口令：**能调色就不生成；能短镜头拼接就不要整段生成。**

### 10.6 成本熔断（写进代码）

- 环境变量 `DAILY_BUDGET_CNY=20`、`PROJECT_BUDGET_CNY=500`  
- 每个 Job 有 `budget_cny_max`  
- 超额：拒绝任务，Agent 用文字建议改用 `photo_look` 或降分辨率  

---

## 11. 分阶段实施（先骨架，再核心功能）

### Phase 0 — 接口与骨架（约 2–3 天）

- FastAPI + 第 6 节全部路由（3D/视频返回 501）  
- `RenderJob` / `Renderer` Protocol  
- SQLite Job 表 + 本地资产目录  
- 空 LangGraph：router 能区分 qa / render  
- 单价表与预算熔断  

**验收**：OpenAPI 可导入；`POST /v1/jobs` 对 `image.2d` 能入队（即使 renderer 是 mock）。

### Phase 1 — RAG + 10 个 Style Pack（约 2 天）

- Chroma 入库  
- `GET /v1/styles` 真实数据  
- `retrieve_style_knowledge` 工具  

**验收**：问「夜景人像用哪个风格」能 cite 到 `hk_night` / `cyberpunk`。

### Phase 2 — 摄影 Look 引擎（约 2–3 天，建议先做）

- 3 个摄影风格 LUT/曲线  
- 前后对比图  
- 多轮「再暖一点」走 param patch  

**验收**：20 张照片套 Look，**0 生图费用**，最像「替代 Pr」。

### Phase 3 — 2D 风格化（约 3–4 天）

- 接一个 Provider（建议硅基流动或万相）  
- 文生图 + 图生图  
- 7 个艺术风格配方调通  
- 失败 playbook + 1 次重试  

**验收**：每风格至少 1 张可放作品集；trace 完整。

### Phase 4 — Agent 闭环打磨（约 2 天）

- 多轮对话、记忆、成本口述  
- 简单前端  
- 评测脚本：10 条固定 query 回归  

**验收**：能完整演示「上传 → 说人话 → 出图 → 改稿 → 说明用了哪条 RAG」。

### Phase 5+ — 双轨（500 元花完之后，另批预算）

- 轨 1：`asset.3d` stub → 真实 Provider  
- 轨 2：`video.clip` → 分镜 → `video.long` 拼接 + 视频 LUT  
- 可选 LoRA Provider  

---

## 12. 未来发展（保留两条方向）

用同一张能力地图，避免做成两个不相干项目。

```
                    ┌──────── StyleForge Agent ────────┐
                    │  Chat · RAG · Job · Cost · Eval  │
                    └───────────────┬──────────────────┘
           ┌────────────────────────┼────────────────────────┐
           ▼                        ▼                        ▼
     现在：2D + Photo Look     轨1：3D 资产              轨2：视频
     image.2d                 单图成模                   短镜头生成
     image.photo_look         风格化贴图                 视频 LUT（复用 Look）
     Style Pack 热加载        风格化静帧预览             分镜 Agent
           │                  可选风格 LoRA              一致性 / 参考帧
           │                        │                    拼接成片（辅助 Pr）
           └──────────── 共享资产库、风格 ID、评测 ────────┘
```

### 12.1 风格扩展

- 新风格 = 新 YAML + ingest，不发版。  
- 摄影类优先加 LUT，艺术类优先加 prompt 配方，失败再 LoRA。

### 12.2 3D 方向演进原则

- 3D 不是「把 Flux 换成 NeRF 就完了」，而是：**几何生成** 与 **风格贴图** 拆开，风格继续复用 2D Pack。  
- Agent 新增工具 `submit_render_job(modality=asset.3d)` 即可，Chat 话术：「把这张水墨图变成可旋转的摆件」。

### 12.3 长视频方向演进原则

- 产品定位从「替代 Pr 调色」升级为「替代 Pr 里重复劳动的那段」：统一 Look、批量套 LUT、按分镜出草稿镜头、粗剪时间线。  
- 专业剪辑、调音、交付仍可回 Pr；Agent 导出 EDL / 拼接 mp4。  
- 成本上，3 分钟成片应大部分是 **调色 + 拼接**，生成只用于缺失镜头。

### 12.4 与招聘的对应（你可以按这个讲）

| 招聘常见要求 | 本项目落点 |
| --- | --- |
| LangChain / LangGraph | 主图编排、Tool、Checkpoint |
| RAG | 风格配方、调色手册、分镜语法、失败 playbook |
| Agent 评测 | critique 节点、固定回归集 |
| 工程化 | FastAPI、Job 状态机、预算熔断、可插拔 Provider |
| 多模态 | 图、Look、预留 3D/视频同一信封 |
| 成本意识 | 单价表、摄影路径 0 推理、长视频禁止整段生成 |

---

## 13. 风险

| 风险 | 应对 |
| --- | --- |
| 风格像套滤镜、辨识度不够 | 一期接受；二期对失败风格 LoRA，而不是现在训 10 个 |
| 图生图不保脸 | `keep_face` + 较低 strength；必要时人脸区域 mask（仍不训练） |
| 吉卜力等 IP 投诉 | 命名与素材双隔离，见硬约束 9 |
| API 涨价 / 欠费 | 双 Provider + 日预算熔断 + Look 引擎托底 |
| 500 元被视频试玩花光 | Phase 5 前禁止默认开启视频 Provider |
| 范围膨胀（又要做完整剪辑器） | 坚持「Look + 草稿镜头 + 导出」，不做时间线 GUI |

---

## 14. 一期验收标准（建议写进 README 勾选）

- [ ] 10 个 Style Pack 可检索，接口 `GET /v1/styles` 可用  
- [ ] 自然语言提交任务，返回 `job_id`，可轮询下载  
- [ ] 摄影 Look 至少 3 种，前后对比，成本为 0  
- [ ] 艺术风格化至少 5 种出可展示图  
- [ ] 多轮改稿不丢 thread  
- [ ] RAG 问答能 cite 风格文档  
- [ ] `asset.3d` / `video.long` 请求被 501 且 schema 校验通过  
- [ ] 累计 API 花费可查询，不超过 500 元配置上限  
- [ ] 任意成功 Job 能还原 prompt/参数/style_id  

---

## 15. 最终建议（执行口令）

1. **现在不训练、不租包月服务器。**  
2. **先冻结第 6 节接口和第 5.3 节 Renderer 协议。**  
3. **先做摄影 Look + RAG + Job，再接 2D 生图 API**——前者免费且更像「替代 Pr」，后者才烧 220 元额度。  
4. **3D 与长视频只实现 stub 与知识占位**，把钱留在 2D 作品质量和 Agent 完成度上。  
5. 等作品集和评测集稳定后，再单独立项：LoRA 或短视频，预算另算。

一期成功标准不是「模型是我训的」，而是：**别人换一个生成后端，你的 Agent、风格知识和任务系统还能用。** 这正是后续转入 3D 和长视频时唯一不会推翻的部分。
